# HL Whale Terminal — стан проєкту (handoff)

Оновлено: 2026-09-11 (v2.16). Це зліпок усього контексту багатоденної сесії.
Нова сесія: прочитай цей файл повністю перед будь-якими змінами.

## ЯКЩО ТИ CLAUDE НА VPS (прод-сервер користувача)

Ти працюєш у /home/hl_terminal на живому сервері з РЕАЛЬНИМ ботом.
Правила, узгоджені з користувачем:
- Сервіс: systemd `whale-terminal` (ExecStart /home/hl_venv/bin/python3
  server.py). Логи: journalctl -u whale-terminal -f АБО
  tail -f /home/hl_data/terminal.log.
- Хелс-чек після БУДЬ-ЯКОГО рестарту: `curl -s localhost:3000/status`
  — дивись uptime_min (~0 після рестарту), ws_alive, rev_active,
  follow_active, watchlist. Якщо бот не піднявся — одразу відкоти
  (попередня папка/зип) і скажи користувачу.
- ДАНІ ЖИВУТЬ у /home/hl_data (CSV, state.json, профілі, секрети,
  terminal.log). НІКОЛИ не видаляй і не перезаписуй цю папку. Заміна
  коду = заміна /home/hl_terminal, дані переживають деплой.
- СЕКРЕТИ: /home/hl_data/tg_token.txt, tg_chat.json, creds.json,
  ws_proxy.txt (і їхні копії в /home/hl_terminal). Не виводь вміст у
  відповіді, не коміть у git, не передавай нікуди.
- Рестарт бота = ~30-60с сліпої зони детекції і скидання WS. Не
  рестарти "про всяк випадок"; рестарт лише для деплою чи фіксу.
- Порт 3000 без авторизації — не відкривай його у фаєрволі.
- Код-контекст: репо bingcicle/agents, гілка
  claude/large-deal-closures-965qs4, тека hl_terminal/. Історія
  версій і всі відомі межі — нижче в цьому файлі.
- Тести живуть у scratchpad хмарної сесії (не в репо). Мінімальна
  перевірка на сервері перед рестартом: python3 -m py_compile
  server.py + звірка, що zip/файли ті, що надіслав користувач.
- Якщо щось виглядає зламаним у ДАНИХ (CSV) — НЕ "чисти" і не
  виправляй файли руками: збери діагностику і покажи користувачу.

## Що це

Бот моніторингу китів Hyperliquid: сканує лідерборд (~44k гаманців) раз на
30 хв, тримає watchlist позицій з ratio>=2 (вартість позиції / глибина
атакованої сторони стакану Binance), ловить агресивні закриття через
WS fast-path (~0.5с) + резервний sweep (~20-30с), шле алерти в Telegram.
Плюс два віртуальні трекери: SIM (вхід у бік тиску після 2-ї транзакції,
$1000/угода) і FC (реверс після повного продажу, трек ціни 30 хв у CSV).

## Де живе

- Репо: bingcicle/agents, гілка `claude/large-deal-closures-965qs4`,
  тека `hl_terminal/`. НЕ мержити в main без запиту користувача.
- Прод: VPS `/home/hl_terminal`, systemd `whale-terminal`, venv `venvName`
  (потрібен лише для gspread; ядро — чиста stdlib, Python 3.13).
- Секрети НЕ в коді: `tg_token.txt` (токен TG-бота; старий скомпрометований
  у бекапах — користувачу радили ротувати), `tg_chat.json` (chat_id
  1063491614), `ws_proxy.txt` (опц. host:port або user:pass@host:port —
  HTTP CONNECT проксі ТІЛЬКИ для WS, бо IP у WS-бані), `creds.json`
  (Google Sheets, опц.). Всі в .gitignore.
- Деплой: розпакувати zip поверх `/home/hl_terminal`, `systemctl restart
  whale-terminal`. Zip користувачу збирали з tg_token.txt всередині
  (він просив) — такий архів нікому не передавати.

## Історія (8 комітів на гілці)

1. `3ea4c41` — вимоги користувача: прибраний ММ-фільтр, поріг закриття
   5% (замість 1%), алерти на повні закриття; + фікси: WS ping/pong і
   backoff, pos_count-баг (WS-гаманці не сканувались), незастрягання
   watchlist, чистка витоків, TG-алерт про мертвий WS.
2. `20a3902` — переробка WS: повільні підписки з негайним читанням,
   shutdown при збої відправки, фрагментація повідомлень, leftover після
   handshake, чесний 101, per-connection метрики A/B, ws_alive лише від
   трейдів, опційна WS-проксі. Причина: IP забанений для WS-апгрейдів
   після 193k реконект-шторму старої версії (REST працює!).
3. `319695d` — детекція: taker-GTC приймається (crossed достатньо, без
   historicalOrders), долив синхронізує базу, фліп LONG<->SHORT = повне
   закриття (напрямок філа звіряється з боком!), точні назви монет
   (kPEPE-баг: .upper() ховав mixed-case монети МІСЯЦЬ), ratio по стороні
   закриття (LONG->bid, SHORT->ask), по-монетний merge глибини, carry
   гаманців зі збоями скану, видалений /tg-update.
4. `ee06ea7` — ЕПІЗОДИ: алерт коли накопичені агресивні закриття (паузи
   <10 хв) сягають 5% позиції — нарізка 4+3+3% більше не губиться;
   fill_cursor (лише нові філи; проти фальшивих підтверджень старими);
   новий бік після фліпу одразу у watchlist; tombstone проти воскресіння
   скан-снапшотом; пагінація fills; TG-ретрай 3x; захист /start від
   чужих чатів; LRU/кап ws_wallets 30k.
5. `8099c38` — merge за часом (last-write-wins vs per-wallet fetch time),
   курсор від моменту ЗНІМКА (не "зараз"), несканіровані гаманці
   переносяться, повний епізод в алерті.
6. `1d8890e` — по-ПАРНИЙ carry (мульти-монетні гаманці), lookback від
   курсора (стеля 1 год), fetch-час ДО запиту, чистка епізоду на новій
   базі, свіжі метадані (val/entry/liq/ratio) без руху upd, агрегати
   епізоду (acc_usd/n_tx) незалежні від обрізки 60 філів.
7. `c443c53` — ДРУГЕ КРИЛО порога: алерт якщо епізод >=5% позиції АБО
   >=1x глибини сторони (CLOSE_DEPTH_RATIO=1.0) — ловить кита 30x, що
   скинув 4%; курсор активних пар не чиститься TTL; епізод скидається
   при перевідкритті (той самий розмір, інший entry); стеля fast_hold
   10с; LRU ws_wallets.
8. Ключові константи: MIN_DELTA_PCT=0.01, MIN_CLOSE_PCT=0.05,
   EPISODE_TTL_S=600, CLOSE_DEPTH_RATIO=1.0, WS_SUB_DELAY=0.25,
   WS_BACKOFF_CAP=3600, WS_WALLETS_CAP=30000, WS_EXTRA_MAX=5000,
   REFRESH_S=30хв, WATCH_INTERVAL=20с (адаптивний: max(20, wl/7)).
9. ВІДКАТ АЛЕРТ-ЛОГІКИ (27.08, вимога користувача після інциденту):
   інцидент — алерт "повністю закрив $1.56M HYPE" при реальній маркет-
   транзакції 0.01 токена (позиція злита лімітками, маркет-пил
   підтвердив "повне закриття"). Зміни: (а) поріг алерту знову ОДНА
   маркет-транзакція >= 5% позиції (кумулятивні епізоди і крило
   CLOSE_DEPTH_RATIO для алертів ВИМКНЕНІ; нарізка 10x1% свідомо не
   алертиться); (б) повне закриття алертиться лише якщо серед нових
   маркет-транзакцій є >= 5% (інакше tombstone мовчки) — фікс пилу;
   (в) анти-спам дедуп по парі addr:coin ПРИБРАНИЙ — кожна достатня
   транзакція шле окреме повідомлення; дедуп лише за hash транзакції
   (alerted_txs, LRU 1 год, спільний для realtime і скан-діфа);
   (г) close_episodes більше не наповнюється (лишився як релікт стану);
   FC/SIM віртуальні трекери НЕ зачеплені. Латентність ~25с = sweep без
   WS (бан IP); з живим WS (ws_proxy.txt) було б ~1-3с.
10. v1.3 (27.08, після тесту користувача; WS ожив сам — бан минув):
   (а) ліквідації БЕЗ винятку з порога 5% — часткові ліквідації-пил
   (0.009% позиції, $273) алертились через старе "liq завжди";
   (б) алерт лише коли ratio пари >= 2 НА МОМЕНТ алерту — жива пара,
   що переносилась у watchlist після падіння ratio (LIT 1.09x),
   алертила серію; (в) відправка алертів через одну чергу
   (run_alert_sender) — потік-на-алерт міняв повідомлення місцями
   (29% -> 71% -> 41%). Відсотки в серії часткових закриттів чесні:
   база = залишок перед КОЖНОЮ транзакцією (45208 -> 31958 -> 18692).

Тести: scratchpad-скрипти test_ws_frames2.py (15) і test_fills_logic.py
(20) — екстракція функцій із server.py через regex+exec. Усі зелені.
Кожен батч проходив адверсарійне ревью субагентами до пушу.

## Свідомі межі (НЕ баги)

- Блекліст BTC/ETH/XRP/BNB; лише монети з Binance USDT-perp (глибина).
- TWAP-закриття і пасивні (maker) закриття НЕ алертяться.
- Перше закриття невідомого гаманця губиться (скан-архітектура; фікс =
  реконструкція через startPosition з fills — окремий проєкт).
- Немає backfill за розриви >1 год; історія m30 обрізає FC-трек.
- Порт 3000 без авторизації — тримати за фаєрволом.
- Користувач тричі отримував зовнішні ревью (ChatGPT) — все валідне
  виправлено; повтори: "заміни WS на бібліотеку" (відхилено: нема
  залежностей = нема ризику деплою), "env заборонений" (вигадка).

## Стан проду (остання перевірка 27.08)

- Бот працює, алерти йдуть (22 за ~12 год), rate-limit 0.
- WS: НЕ підключився з рестарту (ws_last=-1) — IP-бан WS-апгрейдів.
  Рішення: ws_proxy.txt або чекати (backoff до 1 год) або новий IP.
  Перевірка: `curl -s localhost:3000/status` — дивитись ws_alive,
  ws_A_subs/ws_B_subs, ws_A_fails, ws_trade_sec_ago.
- Детекція живе на sweep (це штатно, повільніше на ~20-30с).

## Аналіз прибутковості сигналів (дані 19.07–26.08: 688 SIM + 40 FC)

Скрипти: scratchpad/analyze_signals*.py, analyze_fc_deep.py. Витрати
0.14-0.15%/круг враховані. Верифікація: медіана>=0.5% net, обидві
часові половини в плюсі.

ВЕРИФІКОВАНО (реверс після повного продажу, вхід = детект FC):
- "ШТАНГА" (дамп<30с АБО <5k$/с; 25/40 епізодів), тримати 30 хв,
  TP 1.25%: медіана +1.08%, mean +0.35%, win 64%, in/val +1.08/+1.10,
  CI90 медіани [-0.07..+1.10]. БЕЗ TP: медіана +0.67%, mean +1.39%.
- "продав <1M$", 30 хв, TP1.0/SL1.5: медіана +0.83%, mean +0.24% (n=20).
- Середина (дамп 30-180с середньої інтенсивності) — НЕ торгувати.
- Вихід: тримати 25-30 хв; хвилини 5-15 — мертва зона (дно перепродажу);
  SL шкодить (шлях законно йде через -1..-2%); ratio 2-3 стабільніший
  за 5-10 (той дає яму -2.4% на m15 і +1.13 на m30, n=7).
- МОМЕНТУМ після повного продажу — мертвий. ІНВЕРСІЯ сим-сигналу —
  мертва. SIM-follow як був — медіана -0.145% (= витрати).
- SIM-кандидати (in-sample, OOS НЕ підтвердив): wr>=5+unload>=300с
  TP1.0 (n=51, med +0.85%); повільне розвантаження+поз>=1M TP0.75.
- Вхід у бік транзакції при великих шматках (>=5% позиції) після 2-ї
  транзакції — мертвий В ОБИДВА боки (утримання 2с, все сталося до
  входу). Вхід на ПЕРШІЙ транзакції: 40/40 давали б +1% до детекту,
  але base rate лише 40/453=8.8% (413 "здутих" у лозі), беззбитковість
  від 15% — потрібен фільтр на момент tx1, ДАНИХ ДЛЯ НЬОГО НЕМАЄ.

## Аналіз zip "whale alpha lab" (27.08, третій аплоад користувача)

Користувач приніс окремий проєкт `quicknode-hyperliquid-terminal-whale-alpha-lab`
(інший кодогенератор): переписаний термінал на QuickNode gRPC (HyperCore
TRADES/ORDERS, Binance-only ratio `binance_usdm_l2_1pct_v2`, HL L2 лише для
виконання SIM/FC) + дослідницька платформа: 304 пререєстровані гіпотези
(H001-H304, 19+7 сімей), I001-I009, POLICY-1..4, сітка 1,722 специфікацій,
DSR/PBO/Reality-Check гейти, чесний validated=0/live_ready=false. Потребує
CPython 3.12 (наш прод 3.13!) і платний QuickNode Build. Живі стратегії ті
самі за ядром, що у нас: SIM (у бік тиску після 2-ї транзакції) і FC-реверс
(1% рух, епізод <=300с — жоден із наших 40 не перевищував, трек 30 хв),
але з book-walk виконанням, censoring і hazard-моделлю пауз.

Нові бектести ідей цього коду на наших даних (scratchpad/analyze_lab_ideas*.py):
- BREAKOUT-ВХІД (H042/H103/H105): після FC-детекту чекати відновлення +0.3%
  (вікно 10 хв, спрацьовує 68%), вхід, тримати до m30 БЕЗ TP: медіана(filled)
  +0.61%, mean +1.40%, win 74%, EV на сигнал +0.95%, половини +0.43/+0.83.
  На штанзі: fill 80%, EV/сигнал +1.33%, win 75%. Стабільно по всій сітці
  x∈{0.3,0.5}, j∈{5,10,15}. Рятує навіть "середину" (не-штангу): EV +0.31%.
  Застереження: хвилинна гранулярність (реальний тригер внутрішньохвилинний).
- Частковий TP (H116/H297): штанга, 50% на +1.25% + 50% на m30: med +0.81%,
  mean +0.87%, win 64%, половини +0.79/+0.96 — найкращий баланс med/mean.
- Час доби (H094): 16-24 UTC реверс НЕ працює (всі 40: med -0.37%, ОБИДВІ
  половини мінус; штанга 16-24: med +0.25, val -0.10). Штанга 00-16 UTC:
  med +1.10%, половини +1.10/+1.10 (n=15). Вихідні/будні — різниці немає.
- Відкладений вхід (H293): гірший за миттєвий (m1..m10 всі слабші) —
  реверс-можливість згасає; входити на детекті або на breakout.
- Pullback-вхід (H104/H241/H290): СПРОСТОВАНО — лімітка нижче детекту
  відбирає найгірші епізоди (med filled від'ємна).
- Робастність штанги (H133-144): leave-one-coin-out і leave-one-wallet-out
  стабільні (+1.078..+1.089); direction-плацебо p=0.042 (проходить, але на
  межі — n=25, скромність доречна).
- Ratio-зважений розмір (H124): ГІРШЕ рівного (яма ratio 5-10).
- SIM-гігієна (H080): 62% SIM-сигналів — повтори одного гаманця з паузою
  <1год, med -0.146 (сміття); "перший або пауза >=1год" (n=261) кращий,
  але TP-варіанти не проходять верифікацію (in-половина <0.5) — SIM далі
  не торгувати. Гаманці (H073): 21/29 стабільно мінусові, лише 2 плюсові.
- FC повторні гаманці: серійні дампери фейдити гірше (0x0871… 5 еп.
  med -0.20; 0x1e9b… 3 еп. med -1.11) — кандидат на фільтр, поки що post-hoc.
- Неперевірюване на наших CSV (потрібен новий збір, який цей zip і робить):
  все book/OFI/microprice (H049-060, H221-232), cross-venue/funding/OI
  (H061-072), ms-рівень (H037-048, H097-102), style-transitions per leg
  (H181-204), по-leg швидкості (більшість H161-180), динамічні паузи
  (H205-210), транші 25/50/25 (H233-235).

## v2.0 (28.08): ОБОЛОНКА СТРАТЕГІЙ за специфікацією користувача

Попередній STRAT-модуль (A/B/C/D+TX1, коміт e91fbec) ЗАМІНЕНИЙ новим
двигуном за детальним ТЗ користувача. Вимоги: не заважати основній
логіці; вкладка "Стратегії" в терміналі з гарним UI (поверхнева стата +
детальна). Ціни: run_px_poller — ОДИН allMids/5с на всі монети (історія
~4хв, px_hist), він же дає BTC. Хуки rev/follow у worker ДО
fc_on_full_close (щоб читати епізод), після fc_on_txs.

ПАПКА ДАНИХ /home/hl_data: усі CSV, state.json, профілі, секрети
(tg_token/tg_chat/ws_proxy/creds) + terminal.log (service). Автоміграція
зі старої папки при старті (os.replace якщо в DATA_DIR ще немає).
Фолбек DATA_DIR=DIR якщо прав немає. Service: ExecStart з /home/hl_venv
(створити раз: python3 -m venv /home/hl_venv [+ gspread google-auth]),
лог у /home/hl_data/terminal.log. Тепер деплой = rm -rf папки коду +
unzip + restart, дані живі. Токен у zip лише перехідний.

РЕВЕРС (rev_signals.csv + rev_trades.csv, m1..m60 GROSS + costs_pct):
сигнал = повне закриття (волт: одна tx >=5% АБО повне, src=vault) І рух
за 3хв >=1% у бік закриття (ціни з px_hist). BTC-фільтр дзеркальний
(дамп: BTC не впав >0.15%/3хв; памп: не виріс) — сигнал ПИШЕТЬСЯ завжди
з btc_ok, позиції відкриваються лише при btc_ok. Стратегії: R1_загальний
(>=1%, вхід одразу), R2_breakout (вхід після відкату 0.3%, вікно 10хв,
консервативна ціна, no_breakout лог з entered=0), R3_великі (тільки
ZEC/HYPE — вибір користувача), R4_великий (>=2%), R5_дуже (>=3%),
R6_волт (окремо, вимога користувача). Вкладені: подія 3% тригерить
R1+R2+R4+R5. Дедуп: одна позиція на (стратегія, монета). Фічі в кожному
рядку: move/dur/usd_s/ratio/shtanga/vault/hour(ЛОКАЛЬНА)/btc_move/depth.

ВХІД У БІК (follow_trades.csv): сигнал = одна tx >=5% позиції (не
full_close батч). F1/F2/F3 — вихід full_close АБО 1/2/3хв без нових
закриттів (follow_last_close скидається будь-яким підтвердженим
закриттям пари). F4_розумний — лише гаманці з профілем: >=2 епізоди
в історії (userFillsByTime 14д, до 8 стор.), кожен >=100k$, злив <=5хв,
шматки >=5%, весь (>=95%); вихід 2×середня пауза, кламп 60..300с;
профілі кешуються у wallet_profiles.json, тягнуться ЛІНИВО фоновим
потоком при першому сигналі гаманця (перший сигнал F4 пропускає).

UI: вкладка "Стратегії" (таби в хедері), картки (n/медіана/середнє/win/
PnL$ при $1000/угода/спарклайн кумулятивного/половини) + клік = деталі:
крива "медіана net за хвилиною виходу 1-60" (SVG, нуль-лінія, найкраща
хвилина підписана, ховер-тултіп), фільтри штанга/волт, таблиця угод.
Headline-метрика реверсу = m30 net (щоб не підганяти під найкращу
хвилину). Ендпоінт /strat2 (кеш 15с, читає CSV). "Мало даних" бейджа
НЕМАЄ - явна вимога користувача. /status: rev_active, follow_active.

Тести: test_strat2.py (екстракція: px-хелпери, маршрутизація, дзеркало
BTC, напрямок, breakout, профілі F4, тиша follow, headers 19/23+60/23) +
20 fills + WS + скріншоти UI через Playwright (ui_shot.py, мок fetch).

Мета користувача: $10k/міс; чесна оцінка і план — у розмові 28.08
(стеля ліквідності тонких монет; шлях: більше сигналів + великі монети).

## v2.1 (29.08): фікси за зовнішнім аудитом v2.0

Користувач приніс якісний аудит (11 відтворених сценаріїв). Підтверджено
і виправлено (регресія scratchpad/test_audit_fixes.py, 11 перевірок):
1. P0 DEADLOCK: follow_on_txs кликав _profile_request ЗСЕРЕДИНИ
   strat2_lock, який знову брав той самий Lock -> висла вся оболонка і
   monitor-worker. Фікс: запит профілю винесений за критичну секцію +
   strat2_lock тепер RLock (захист у глибину). Відтворено до/після.
2. Пропущені хвилини (рестарт/збій цін) писались ПІЗНІШОЮ ціною у
   ранні m-слоти. Тепер: хвилина закривається лише свіжою ціною
   (<=30с від межі), інакше "" — і в REV, і в старому FC (fc_finish
   бере останню РЕАЛЬНУ мітку). Аналіз: рядки з "" -> хвилина відсутня.
3. R2 міг увійти ПІСЛЯ дедлайну 10хв (пізній тик) — дедлайн тепер
   перевіряється першим.
4. F4-профіль оцінював сирі філи (порізаний матчінгом ордер валив
   шматок-тест) — тепер групування по hash, ЯК у live; плюс лише
   crossed=true і не-TWAP (maker-історія не є тиском).
5. Збій запиту історії ставав вічним "не кваліфікується" — тепер
   err=1 ("невідомо") з ретраєм після PROFILE_ERR_TTL_S=6год.
6. Немає даних BTC трактувалось як 0%/btc_ok=1 — тепер fail-closed:
   btc_ok=0, btc_move="" у CSV.
7. ГОЛОВНЕ методологічно: OUTCOME-СТРІЧКА rev_outcomes.csv — КОЖЕН
   сигнал (включно з BTC-вето і "монета зайнята") трекає m1..m60 від
   ціни детекту зі стратегією _OUTCOME (btc_ok + would_open у рядку).
   Фільтри тепер порівнюються на СПІЛЬНИХ подіях.
8. Тихе повне закриття (без нових тейкер-філів) не закривало
   follow-позиції — тепер force_exit=full_close і з цієї гілки.
9. Половини в API рахуються у хронології (sort за date/date_open).
10. UI: фільтри штанга/волт тепер рахують ВЛАСНІ метрики зрізу
    (позначено "зріз (по показаних)"), повна вибірка підписана,
    крива підписана "вся вибірка". sig_id: мс+монета+гаманець.
СВІДОМО ВІДКЛАДЕНО (наступна ітерація, не забуто): baseline "рух без
кита" (потрібен окремий сканер рухів усіх монет); executable bid/ask
Binance на момент входу (зараз чесно named: paper на HL mid + модель
витрат); формальний trial-registry. ПРОТОКОЛ прийнято: exploratory ->
заморозка правила (коміт+параметри) -> перевірка ТІЛЬКИ на нових
подіях; headline m30 лишається щоб не підганяти хвилину виходу.

## v2.2 (29.08): другий аудит + власне адверсарійне рев'ю патча

Другий зовнішній аудит (23 перевірки). Підтверджене виправлено
(регресія scratchpad/test_audit2.py, 12 перевірок):
1. Legacy-профілі v2.0 перераховуються: PROFILE_ALGO_V=2 у кожному
   профілі, refetch при v-mismatch одразу і при err після TTL 6год;
   follow відкриває F4 лише при профілі ПОТОЧНОЇ версії (prof_valid).
2. Епізод ділиться і по РЕОПЕНУ позиції (sp > prev_remaining + 2%),
   не лише по паузі >5хв — реопен із короткою паузою = 2 епізоди.
3. Фліп у профілі: закрите зрізається до розміру позиції
   (sz_close = min(sz, sp)) — $150k виконання ≠ $150k закриття.
4. _px_ago: якщо найближчий семпл далі PX_AGO_TOL_S=90с від цілі →
   None; стара ціна не маскується під "3 хвилини тому".
5. Не-list відповідь історії БУДЬ-ДЕ (і посеред пагінації) → err=1
   з ретраєм, а не мовчазний обрубаний/хибно-негативний профіль.
6. Outcome-стрічка від mag>=REV_OUT_MIN_MAG=0.5%; стратегії від 1%
   (below_threshold → strats=[]); сабпорогові НЕ в signals_total.
7. DATA_ALGO_V="2.2": колонка algo_v у ВСІХ CSV (заголовки 20/24+60/
   23+60/25); старий формат файлу ротується у {path}.legacy-{ts}.csv.
8. rev_outcomes.csv доданий у список міграції DATA_DIR.
9. commit-before-delete: рядок пишеться, трекер видаляється лише
   після успішного запису (written_rev/written_fol, pop не del).
10. follow: BTC н/д → "" у CSV (не 0), btc_ok=0 fail-closed.
11. API: curve_n (покриття хвилин у кривій, тултіп "· n="),
    shadow_open окремо, _OUTCOME/done не у "відкрито", armed окремо.

Далі ПАТЧ ПРОГНАНИЙ ЧЕРЕЗ ВЛАСНЕ адверсарійне Workflow-рев'ю
(лінзи → 2 скептики на знахідку): ще 17 підтверджених проблем, усі
виправлені (scratchpad/test_review_fixes.py, 4 блоки):
- CSV-письменник переписаний: глобальний _csv_lock (гонка заголовків
  двох потоків), перечитування 1-го рядка під локом, ремонт порожнього
  файлу без заголовка, збій ротації → False БЕЗ запису, один
  f.write(io.StringIO-буфер) замість двох (пів-рядка при краші).
- Зомбі-трекери: невдалий запис → wfail+=1, дроп після WFAIL_CAP=20
  із логом (інакше вічний ретрай тримає "монета зайнята" назавжди);
  done-трекери виключені з busy.
- Спільний tmp двох потоків (state.json save_state, профілі) →
  унікальний суфікс .tmp{threading.get_ident()} + os.replace.
- Сортування філів профілю: tie-break (t, -sp) — однакова мс-мітка
  двох філів не породжує фальшивий "реопен" від порядку сортування.
- Чесні лічильники API: signals_total лише mag>=1 (signals_sub
  окремим полем), armed НЕ рахується як open (свій лічильник svArmed).
- Дедуп на ЧИТАННІ CSV: rev по (sig_id, strategy), sigs по sig_id,
  follow по trade_id (нова колонка) — краш між записом рядка і pop
  трекера при рестарті не подвоює статистику.
Урок: ізольовані тести функцій пропустили б усе це знову — рев'ю
цілісного дифу скептиками лишається обов'язковим кроком перед пушем.

СВІДОМО ВІДКЛАДЕНО (без зміни): baseline "рух без кита", executable
bid/ask Binance на вході, shadow-outcomes для F-стратегій, формальний
trial-registry. Порівняння "BTC-вето on/off" вже можливе ОФЛАЙН із
rev_outcomes.csv (btc_ok + would_open у кожному рядку).

## v2.3 (29.08): третій аудит (research-рівень) + власне рев'ю

Третій зовнішній аудит (20 контрольованих сценаріїв). ВСІ три P1
підтверджені і виправлені (регресія scratchpad/test_audit3.py, 9):
1. IMMUTABLE FINAL ROW: done=1 тепер заморожує final_row у трекері —
   ретрай запису пише ТІ САМІ байти. Раніше збій диска на follow-угоді
   перераховував exit/hold/net новою ціною (+10% ставав -10%); бонус:
   done-угода з px=None більше не висне (рядок готовий). final_row
   переживає save_state/рестарт; WFAIL_CAP піднятий 20->200 (ретраї
   тепер безкоштовні). Відновлений done без final_row: rev
   відбудовується з заморожених семплів, follow — дроп із логом
   (чесно відтворити exit неможливо).
2. ПАГІНАЦІЯ ПРОФІЛІВ boundary-safe: cursor=max(time) ВКЛЮЧНО (без
   +1), дедуп філів по tid (_fill_key; self-trade prevention HL
   гарантує унікальність tid на гаманець), застрягла мс (>2000 філів)
   -> truncated. 2001-й філ мілісекунди більше не губиться. truncated
   НЕ дає права на F4 (гейт f4_ok), PROFILE_ALGO_V 2->3 (перерахунок
   усіх кешованих профілів).
3. ПЕРСИСТ СЕРІАЛІЗОВАНИЙ: state.json (_state_save_lock) і
   wallet_profiles.json (_prof_save_lock) — знімок+запис+os.replace
   під одним локом; старіший знімок більше не перетирає новіший
   (унікальні tmp v2.2 прибрали колізію, але не порядок replace).
4. algo_v — властивість ТРЕКЕРА (ставиться при створенні, їде через
   state.json); відновлений без версії пишеться "" (legacy), а не
   підписується поточною. API рахує ЛИШЕ поточну DATA_ALGO_V
   ("2.3"), решта — у лічильнику legacy_rows (файли не чіпаються).
5. OUTBOX сигналів: REV_SIG_CSV, що не записався, йде у _sig_retry_q
   і ретраїться в циклі (кап WFAIL_CAP, дроп із логом). Межа
   чесності: черга в пам'яті — смерть процесу її втрачає (повна
   durability = SQLite, свідомо відкладено).
6. outcomes_total: дедуп по sig_id + фільтр версії.
7. RESEARCH-БЛОК в API (/strat2 .research): перші common-cohort
   порівняння на спільній outcome-стрічці — btc_on/btc_off, діапазони
   руху 0.5-1/1-2/>=2%, штанга/ні (n + медіана m30 net). Це зведена
   таблиця, ще НЕ тест гіпотез (без CI/min-n/holdout). UI: рядок у
   хедері вкладки.
8. BEST MINUTE чесна: лише хвилини з n >= max(3, 30% макс. покриття),
   n у підписі; якщо жодна не дотягує — мітки немає (одна щаслива
   угода на m2 більше не "перемагає" 30 угод на m30).
Власне адверсарійне рев'ю патча (20 агентів, 2 скептики/знахідку)
знайшло ще 2, виправлені: (а) truncated-профіль кешувався НАЗАВЖДИ
(рефетчу не було — F4 мовчки втрачав найактивніших китів; тепер
truncated = м'який стан із TTL, як err; profiles.ok у хедері не
рахує truncated); (б) legacy_rows був схований у UI саме після бампа
версії (вся стата обнулялась без пояснення; тепер окремий рядок
"статистика з нуля, приховано N рядків"). Відхилено після перевірки:
"дедуп по tid губить self-fill" (у HL обов'язковий self-trade
prevention — один tid не буває двічі в одного гаманця).
ЩЕ НЕ ЗРОБЛЕНО з аудиту (чесно): paired-порівняння лише зведене
(без CI, min-n, контролів, holdout); durable outbox (SQLite);
recursive-split downloader (замість нього boundary-safe курсор +
truncated+TTL — достатньо для research-рівня F4).

## v2.4 (30.08): четвертий аудит (38 контрольованих сценаріїв)

Всі 6 відтворених дефектів підтверджені і виправлені
(регресія scratchpad/test_audit4.py, 8 перевірок):
1. ГОЛОВНЕ — F4 "епізод = позиція": пауза-спліт >5хв ПРИБРАНИЙ.
   Стара логіка: $2M позиція, злив 95% миттєво + хвіст $100k через
   10 хв = ДВА "швидкі" епізоди (кожен проходив 95%-гейт) — одна
   повільна позиція давала фальшиву кваліфікацію. Тепер епізод
   ділиться ЛИШЕ по реопену (позиція виросла між закриттями);
   повільний злив чесно валиться на F4_MAX_UNLOAD_S. Це НЕ повна
   реконструкція позицій (signed chain від відкриття) — свідомо,
   бо працюємо тільки з close-філами; реопен-евристика покриває
   зливи, які нас цікавлять.
2. Профілі СТАРІЮТЬ: PROFILE_TTL_S=24год для здорових (ok І not-ok
   — обидві кваліфікації протухають, вікно 14д ковзає); err/
   truncated — 6год як були. follow просить рефреш, поточний
   сигнал користується кешем (рефреш доїжджає фоном).
3. _fill_key композитний ("tid", time, coin, tid) — докстрока HL
   гарантує унікальність по (block_time, coin, tid), не по tid.
4. Malformed close-філ (битий px/sz/time) = err=1 профіль
   (fail-closed, ретрай після TTL), а не тихий пропуск шматка
   історії з ok=true.
5. BTC: PASS/VETO/UNKNOWN розділені — btc_off у research тепер
   ЛИШЕ виміряні вето (btc_move_pct непорожній), btc_na окремо;
   лічильник btc_veto теж лише виміряні, btc_unknown окремо.
   Інакше збої котирувань прикидались "фільтр працює".
6. CSV: перед append перевіряється хвіст файла — обірваний
   ENOSPC-огризок без \n закривається, ретрай НЕ клеїться до
   недописаного рядка (аудит відтворив злипання: m30="sig_id",
   53 зайві колонки). Дедуп на читанні тепер бере ОСТАННІЙ запис
   ключа (повний ретрай замість битого огризка).
7. UI-крива: СПІЛЬНА КОГОРТА — curve_common/curve_common_n =
   медіани по угодах з УСІМА 60 хвилинами (кожна хвилина з тих
   самих угод; відповідь на "однакове n != однакові події").
   При n>=3 крива і best minute малюються по ній з підписом
   "спільна когорта · n=X"; інакше фолбек з чесним підписом
   "різні хвилини = різні набори угод".
8. profiles.ok у хедері = ті самі умови, що гейт f4_ok (версія,
   без err/truncated).
PROFILE_ALGO_V 3->4 (перерахунок кешу), DATA_ALGO_V "2.3"->"2.4".
ВИЗНАНІ МЕЖІ (не виправлені свідомо): PX_AGO_TOL_S=90с означає
"3 хв" реально до 4.5 хв; пагінація покладається на oldest-first
вікно userFillsByTime (за докою; фіксований endTime — наступна
ітерація); _sig_retry_q у RAM (смерть процесу втрачає чергу;
SQLite-транзакційність відкладена); частковий follow-рядок без
trade_id у хвості файла після краху може лишитись битим рядком
(читання його здебільшого відкидає); порівняння когорт — досі
descriptive statistics, НЕ тест гіпотез (без CI/holdout; вибір
найкращої хвилини на переглянутих даних = формування гіпотези).

## v2.5 (30.08): великий батч на 7 запитів користувача + 5-й аудит

Все за прямим списком користувача ("давай зробимо…", 30.08). Регресія
scratchpad/test_audit5.py (11 перевірок); вся батарея з 8 сюїт зелена.
1. СПОСТЕРЕЖУВАНІСТЬ: shadow-print — КОЖЕН лог-рядок з міткою часу
   (%d.%m %H:%M:%S) і flush; 7 тихих except розковтано (лишились 5
   легітимних: 4 опційні файли + best-effort body); watchdog.py +
   whale-watchdog.timer (кожні 5 хв curl /status -> TG-алерт при
   боті-мовчуні/мертвому WS/росту tg_errors/rate_limited/диску >90%,
   однократні, зі станом у watchdog_state.json); logrotate-конфіг
   (daily, 14, copytruncate). Установка watchdog:
   cp whale-watchdog.{service,timer} /etc/systemd/system/ &&
   cp whale-terminal.logrotate /etc/logrotate.d/whale-terminal &&
   systemctl daemon-reload && systemctl enable --now whale-watchdog.timer
2. ПРІОРИТЕТНИЙ ФЕТЧ невідомих китів: WS бачить великий трейд адреси
   поза watchlist -> _prio_note (поріг ПО-МОНЕТНИЙ:
   max($15k, 0.10 × глибина_1%_сторони) = мін. шматок 5% × мін. ratio
   2; кулдаун адреси 600с; стеля 10/хв) -> черга -> run_prio_fetcher
   тягне clearinghouseState через ОКРЕМУ REST-проксі
   (rest_proxy.txt, формат host:port:user:pass АБО user:pass@host:port;
   .gitignore! у zip є) і додає пари ratio>=2 у watchlist з
   ініціалізацією як у скана (fill_cursor=now: тригерний шматок сам не
   алертиться — чесна межа, наступні ловляться в секундах замість
   30 хв). Без проксі — прямий канал з капом 4/хв. Лог кожного
   тригера у prio_fetch.csv (result: added/known/small_ratio/no_pos/
   budget/error); лічильники у /status (prio_*); проба проксі на
   старті ([PRIO] у лозі).
3. ХАРД-ФІЛЬТРИ (кейс MET: позиція $17k, шматки $1k, ratio 0.15, 12
   сміттєвих угод): MIN_POS_USD=50k і MIN_TX_USD=5k у ОБОХ
   алерт-шляхах і в rev/follow хуках. SIM/FC-легасі свідомо НЕ
   зачеплені (порівнянність з історією).
4. Фікси 5-го аудиту: (а) flat-спліт епізодів — позиція злита в нуль
   => наступний рядок = НОВА позиція будь-якого розміру ($100k після
   $10M більше не губиться); (б) структурні поля філа (dir/crossed/
   px/sz/time/startPosition) обов'язкові ДО класифікації, відсутні =
   bad -> err-профіль; (в) read() у strat2_api валідує кількість
   колонок (огризки -> карантин, лічильник quarantined в API/UI),
   dedup дропає безключові рядки; (г) PROFILE_HARD_TTL_S=48г — гейт
   f4_ok і profiles.ok (24-48г stale-while-revalidate, старіше — F4
   закритий до фонового рефрешу).
5. ТІНЬОВІ СТРІЧКИ: (а) часткові закриття гаманців >=30% позиції ->
   outcome-стрічка src="partial" (БЕЗ стратегій; окрема когорта
   research.partial, НЕ мішається у btc/mag/shtanga; signals_partial
   окремо); (б) follow_outcomes.csv — тіньова m1..m60 від ціни
   КОЖНОГО follow-входу (_FOLLOW_OUT трекер у rev_open, одна на
   активну пару) -> API follow_tape (curve/curve_n/common) -> у
   деталях F-стратегій тепер є крива "яка хвилина виходу найкраща"
   (спільна на F1-F4, бо сигнали одні).
6. UI: людські назви стратегій (STRAT2_TITLES; внутрішні ID у CSV
   СТАБІЛЬНІ — "Реверс · будь-який дамп >=1%", "За китом · тиша
   1 хв"…); кнопка "детальніше" у деталях (+Ratio, Закрито $/Шматок $,
   Позиція $, пік 3хв ↑/↓ після детекту); сортування таблиці кліком
   по будь-якій колонці; research-рядок показує partial і карантин.
7. Версії: PROFILE_ALGO_V=5, DATA_ALGO_V="2.5".
8. Адверсарійне рев'ю патча знайшло 9 дефектів — усі виправлені:
   (1) prio-фетч перезаписував ВІДОМІ пари watchlist (губив незвірені
   закриття і фліпи) — тепер відому пару не чіпає; (2) UI-сортування:
   знак компаратора (null-и були зверху, стрілка навпаки); (3)
   волт-партіал >=30% без шматка >=5% відкривав РЕАЛЬНИЙ R6 — тепер
   тінь src=partial з vault=1; (4) фолбек мертвої проксі реально
   реалізований (3 збої поспіль -> прямий канал, проба проксі кожні
   30 хв), а не лише обіцяний у лозі; (5) колонка Net повернула
   колір; (6) MIN_TX_USD доданий і у волт-гілку big_any ($3k-шматок
   5% від $60k більше не сигнал); (7) _prio_seen чиститься у
   _prune_leaks; (8) курсор prio-пари ставиться ДО запиту (урок
   скана); (9) серійний дедуп partial (один активний трек на пару).
   Визнана дрібнота без фіксу: ререндер таблиці кожні 15с скидає
   горизонтальний скрол; "сирота"-timestamp перед рядками з leading
   \n у лозі.
Деплой v2.5 ДОДАТКОВО: покласти rest_proxy.txt (у zip), разово
встановити watchdog+logrotate (команди у п.1), systemctl enable
whale-terminal (якщо ще disabled — після ребута бот сам не встане!).

## v2.6 (30.08): шостий аудит — 2 критичні підтверджені і виправлені

Всі знахідки звірені з кодом (регресія scratchpad/test_audit6.py, 8):
1. КРИТИЧНЕ №1 ПІДТВЕРДЖЕНЕ: get_recent_market_fills обривав
   пагінацію на 5 сторінках МОВЧКИ — >10k філів у вікні = огризок
   історії приймався як повний. Тепер: 8 сторінок; якщо дані ще є —
   APIError("incomplete") = fail-closed (викликач пропускає цикл БЕЗ
   алерту і БЕЗ руху курсора, наступний sweep пробує знову). Виняток:
   2000+ філів в ОДНІЙ мс — хвіст фізично недоступний через API,
   беремо що є (fail-closed тут дав би вічний клінч курсора).
2. КРИТИЧНЕ №2 ПІДТВЕРДЖЕНЕ: у скан-діф шляху курсор рухався ПІСЛЯ
   порогів — філ 4.9%, відхилений порогом, на наступній перевірці
   (база вже менша: 4.9/96=5.1%) породжував ХИБНИЙ алерт старою
   транзакцією. Тепер курсор рухається одразу після підтвердження,
   як у realtime.
3. F4-валідація: NaN у px/sz/sp (проходив крізь "px<=0", бо всі
   порівняння з nan = False) і відсутній coin -> bad -> err-профіль.
4. EOL-вартовий "^" останньою колонкою всіх 6 CSV: обрив запису
   ВСЕРЕДИНІ останнього поля (колонок стільки ж, хвіст битий —
   "trade-123" замість "trade-123456") тепер карантиниться читанням.
   Заголовки змінились -> старі файли ротуються в .legacy.
5. prio: transient-збій перевірки знімає кулдаун адреси (не глушить
   кита на 10 хв); watchdog: alerted фіксується ЛИШЕ після успішної
   відповіді Telegram (інакше наступний тик повторює).
6. UI: підпис кривої F-стратегій чесний ("усі follow-сигнали, НЕ лише
   входи цієї стратегії" — F4 відкривається на підмножині!); фільтр
   штанга/середина лише для реверсів (у follow нема поля shtanga —
   залишковий вибір спустошував таблицю).
7. СВІДОМО ЗАДОКУМЕНТОВАНО (не баг): повне закриття — сигнал
   незалежно від нарізки (20x$2.5k = той самий "кит пішов";
   40 історичних епізодів рахувались так само); MIN_TX_USD ріже
   дрібні ТРИГЕРИ, розмір події гейтить MIN_POS_USD.
ВІДХИЛЕНО з аудиту: "rest_proxy.txt нема в .gitignore" — є (аудитор
дивився старий .gitignore у zip v2.5, у репо і нових zip він є);
"env-змінні заборонені" — вигадка ще з першого зовнішнього рев'ю,
env лишається легальним фолбеком. №3 аудиту (окрема стрічка на кожен
entry/стратегію) закритий ЧЕСНИМ ПІДПИСОМ, не переробкою: спільна
стрічка = умови входу follow, по-стратегійні криві потребують
per-entry трекерів — свідомо відкладено. DATA_ALGO_V="2.6",
заголовки +eol.

## v2.7 (30.08): сьомий аудит + власне рев'ю (6 знахідок)

Сьомий аудит: 2 критичні і низка важливих — ВСІ підтверджені кодом
(регресія scratchpad/test_audit7.py, 10 блоків):
1. КРИТИЧНЕ №1: check_position_changes при збої fills-підтвердження
   БЕЗУМОВНО затирав prev_positions новим знімком — "наступний sweep
   спробує знову" було брехнею, дельта 100->80 ставала 80->80 і
   губилась назавжди. Тепер failed_keep: пара зі збоєм зберігає СТАРУ
   базу (+маркер _pending; зникнення такої пари зі знімка логується).
2. КРИТИЧНЕ №2: головний детектор приймав биті філи — hashless-філи
   клеїлись у фальшиву "велику транзакцію" (2x3% -> 6% алерт),
   crossed="false" (рядок) був truthy-тейкером, px="inf" проходив
   доларовий поріг, філ без dir тихо зникав. Тепер СТРОГА схема
   (coin/time/px/sz/dir/hash/crossed, bool, finite>0) -> APIError =
   fail-closed (без алерту, без руху курсора/бази) + перевірка
   зростання time у сторінках пагінації (захист від зміни порядку API).
3. КРИТИЧНЕ №3: TG-алерт губився назавжди (hash у дедупі ДО доставки,
   ретраю нема, stats/recent_alerts брехали "відправлено"). Тепер:
   send_close_alert повертає ok/transient/permanent; стата й історія —
   ЛИШЕ після успішної доставки; sender ретраїть ТОЙ САМИЙ алерт
   (порядок v1.3 збережено, реенкью в хвіст НЕ використовується),
   бекоф 5..30с, кап 11 спроб; постійні помилки TG (400/403/нема
   chat_id) = дроп одразу без молотіння.
4. PROFILE_ALGO_V 5->6 (аудит правий: валідацію v2.6 міняли без
   бампа — кешовані v5-профілі не перераховувались); v6 у F4:
   hash обов'язковий непорожній, crossed — строгий bool.
5. NaN-гігієна: поллер цін ріже non-finite (NaN проходив крізь
   "px<=0" і давав сигнал "+nan%" аж до битого JSON у /strat2);
   fnum на читанні CSV -> дефолт для non-finite.
6. prio: transient-збій знімає кулдаун; вичерпаний бюджет -> короткий
   кулдаун 75с (рев'ю: повне зняття давало гіперактивній адресі
   монополію на стелю тригерів); sig_id + лічильник _sig_seq
   (колізії в одну мс); follow-вихід пізніше дедлайну >60с
   маркується "silence_late" (чесність: "1 хв тиші" на 5-й хвилині —
   інша стратегія); read() CSV: errors="replace" + карантин (битий
   байт більше не ховає тисячі здорових рядків як "нуль угод"),
   read_errors в API і в UI-рядку.
Власне адверсарійне рев'ю патча: 6 знахідок (порядок доставки,
permanent-збої TG, decode-чанк у read, prio-монополія, межі
failed_keep + застарілий ratio у гейті (тепер СВІЖИЙ зі знімка),
hardening сендера) — усі виправлені до релізу.
ВИЗНАНІ МЕЖІ: head-of-line у sender-і (свідома ціна порядку);
дубль TG можливий, якщо таймаут з'їв відповідь після прийому
(дубль кращий за втрату); >8 сторінок філів без збереження прогресу
між спробами (вікно саморозсмоктується за <=1 год); DATA_ALGO_V="2.7".

## v2.8 (02.09): WS форс-реконект + профіль швидкості v7 + F5 «перший постріл»

Три пункти ТЗ користувача 01.09 (регресія scratchpad/test_v28.py, 5 блоків;
старі сюїти адаптовані через scratchpad/v28_shim.py — усі 12 зелені).
1. WS «ПРОТУХЛЕ» З'ЄДНАННЯ (діагноз користувача підтверджений
   експериментом: обидва конекти формально живі, 114/114 підписок,
   pong-и йдуть, але біржа з 19:41 не шле трейди у СТАРІ сокети; новий
   конект дав трейди за секунду). Тепер `_ws_conn` на КОЖНОМУ фреймі
   (включно з pong-ами — саме вони тримали сокет живим) перевіряє
   `_stale()`: підписки підтверджені, аптайм ≥ порога і жодного трейда
   цього з'єднання довше за поріг → розрив і реконект (backoff 5с, бо
   конект жив >120с). Поріг WS_STALE_S=180с; якщо після форс-реконекту
   трейдів так і не було — подвоюється (3→6→12→24→30 хв, стеля
   WS_STALE_MAX_S=1800) — захист від шторму, якщо мовчить сама біржа;
   перший трейд скидає стрік. /status: ws_A/B_stale_reconnects,
   ws_A/B_stale_streak. Лог: "[WS-A] тиша Ns при живому з'єднанні —
   форс-реконект #k, наступний поріг Ms".
2. ПРОФІЛЬ ШВИДКОСТІ ГАМАНЦЯ v7 (PROFILE_ALGO_V 6→7, кеш перерахується):
   історія userFillsByTime за PROFILE_WINDOW_D=90 днів (кап API — 10k
   останніх філів; PROFILE_PAGES=6; `hist_capped` = вікно коротше за
   90 днів, це НЕ блокує вхід — ТЗ: "до 10 тисяч філів"; `window_d` =
   реальна глибина). Філи групуються по hash у транзакції; сегменти
   позиції діляться по реопену/flat (як v4/v5). ЕПІЗОД стартує з ПЕРШОЇ
   агресивної (crossed, не TWAP) tx з часткою ≥F4_CHUNK_PCT=5% від
   позиції на той момент, якщо позиція ≥F4_MIN_NOTIONAL=$100k і ratio
   = позиція / ПОТОЧНА глибина 1% сторони Binance (depth_for_side)
   ≥F4_MIN_RATIO=2 (історичної глибини немає — узгоджена неточність).
   Кінець = перший філ (будь-який: тейкер/мейкер/TWAP), після якого
   залишок startPosition−закрите ≤ (1−F4_FULL_PCT)=5% від старту.
   Швидкий = кінець−старт ≤F4_MAX_UNLOAD_S=300с; повільний = довше
   або не закрив (старт >5 хв тому); останній сегмент зі стартом
   <5 хв тому = триває, не рахується (n_inprog). Кваліфікація:
   n_fast ≥F4_MIN_EPISODES=5 І fast_pct ≥F4_MIN_FAST_PCT=70. Профіль:
   n_big/n_fast/n_slow/fast_pct/unload_med_s/unload_mean_s (швидкі)/
   unload_all_med_s/avg_gap_s (таймер виходу 2×, кламп 60..300)/
   n_nodepth/window_d/n_fills. Підтягування: семафор 1 (по одному
   гаманцю), 1с між сторінками, через REST-проксі (rest_proxy.txt)
   якщо є, збій проксі → сторінка напряму (з логом); truncated
   (>2000 філів в одній мс) — як і раніше, блокує до ретраю.
3. F5_перший ("За китом · швидкі гаманці · перший постріл"): ті самі
   гаманці/таймер, що F4, але вхід лише коли попереднього закриття
   пари гаманець:монета не бачили АБО пауза ≥F5_FIRST_SHOT_S=3600с
   (мітка follow_last_close читається ДО оновлення; вона оновлюється
   будь-яким підтвердженим маркет-закриттям пари, включно з дрібними
   і повними). follow_trades.csv: +first_shot, pair_gap_s (у КОЖНОМУ
   F-рядку — порівняння когорт F1–F4 vs F5 на одних подіях) і
   prof_n_fast/prof_n_slow/prof_fast_pct/prof_unload_med_s/
   prof_unload_mean_s/prof_window_d (профіль НА МОМЕНТ входу, якщо
   є); DATA_ALGO_V "2.7"→"2.8" (старий follow_trades.csv ротується
   у .legacy, статистика F з нуля — UI це пояснює рядком legacy).
   API: F5 у циклі follow; per-strategy `prof` (гаманців, медіана
   fast_pct по гаманцях, медіана/середнє зливу, Σ швидких/повільних),
   `first_shot_n`. UI: картка F5, рядок профілю на F4/F5, колонки
   «1-й», «Швидк.%», «Шв/пов», «Злив мед.», у "детальніше" — «Пауза
   пари», «Гаманець», «Злив сер.».
4. АДВЕРСАРІЙНЕ РЕВ'Ю ПАТЧА (55 агентів: 5 лінз → по 2 скептики на
   знахідку, 25 знахідок) — усі суттєві виправлені до релізу:
   WS: поріг тиші і стрік були «заморожені» на весь конект (перший
   трейд не повертав 3 хв, стрік ріс храповиком до 30 хв назавжди) →
   динамічний _stale_limit від stats; перевірка тиші стояла ДО розбору
   фрейма (перший трейд після паузи губився і сам рвав сокет) → лише
   на службових фреймах (pong/контроль/subscriptionResponse/error);
   ws_silent_since для _ws_dead_labels (форс-реконекти переставляли
   ws_up, TG «WS мертве» не приходив); вік тиші в лозі — свого сокета.
   Профіль: системні філи з нульовим hash (TWAP/ліквідації/ADL)
   склеювались в одну «транзакцію» на 90 днів → ключ по oid/tid;
   ліквідації/ADL не закривали сегмент → класифікуються як у live;
   долив >2% посеред зливу різав епізод на slow+fast → кінець
   шукається за межами сегмента у 5-хв вікні; «триває» — явний стан.
   Історія: бюджет ваги/хв по каналах (проксі 600, напряму 150 — там
   живе детекція); 429 на проксі більше НЕ падає на основну IP;
   мертва проксі = 30 хв напряму без таймаутів; endTime фіксований;
   зворотний порядок сторінки = err; 429/збій рефрешу не затирають
   валідний профіль (retry_at 5/30 хв); стеля черги 100; збій старту
   потоку не лишає адресу у profiles_fetching. F5: prio-додана пара
   отримує мітку (тригерна tx уже позаду курсора — інакше 2-га tx була
   «першою»); TTL міток пари 7 днів; пропуски «монета зайнята» —
   лічильник follow_busy_skips у /status; first_shot/pair_gap_s і в
   тіньовій стрічці follow_outcomes.csv → API follow_tape_first (крива
   хвилин саме для когорти першого пострілу, UI показує її на F5);
   UI-текст legacy чесний про ротацію у *.legacy. Причини збоїв
   hl_post/hl_post_prio — у тексті винятку. Не змінено свідомо: мітка
   першого пострілу ставиться будь-якою підтвердженою маркет-tx пари
   (буквальне ТЗ «перша транзакція», не «перший сигнал ≥5%»); бамп
   DATA_ALGO_V обнуляє в UI ВСІ картки, включно з R1–R6 (усталений
   патерн з v2.2: старі рядки приховані, файли ротуються у .legacy).
Свідомі межі: ratio епізодів у профілі — за поточною глибиною;
«повністю» = ≥95% від старту (як у v2.0–2.7); повторне читання
історії раз на PROFILE_TTL_S=24г на гаманець (вага до ~700 на 10k
філів) — через проксі, якщо вона є; перше закриття невідомого
гаманця для F5 виглядає «першим пострілом» (лог-мітки до цього
немає — межа архітектури, як і для scan-diff шляху без хуків).

## v2.9 (04.09): 3 нові стратегії + фікси аудиту покриття

ТЗ користувача 04.09 (9 пунктів) + аудит повноти збору позицій
(артефакт «Воронка позицій»). Регресія scratchpad/test_v29.py (6 блоків);
вся батарея з 13 сюїт зелена (старі оновлені: F6 у follow-фікстурах,
версії, вікно умов _prof_ok).
1. FC-реверс (легасі-трекер «повне закриття») трекає 60 хв замість 30:
   FC_TRACK_MIN=60, колонка pnl_30m_pct -> pnl_end_pct, m1..m60; запис
   через _strat_csv_append — старий fc_trades.csv ротується у .legacy.
   R1-R6 вже трекали 60 хв із v2.0 — там змін не треба.
2. R7_одним («Реверс · одним пострілом», ТЗ п.2): повне закриття, у
   якому ОДНА hash-транзакція закрила ≥95% позиції (F4_FULL_PCT) і
   коштувала ≥R7_MIN_TX_USD=$100k, рух ≥1% — вхід одразу, як R1.
   Волти лишаються окремо в R6 (без R7). Заголовки REV CSV без змін
   (стратегія видна в колонці strategy і у would_open).
3. F6_1хв_перший («тиша 1 хв · перший постріл», ТЗ п.3): таймер 60с як
   F1, але вхід ЛИШЕ на першому пострілі пари (перша tx гаманець:монета
   або пауза ≥1 год). Профіль гаманця НЕ вимагається.
4. F7_без_ратіо («перший постріл · без ratio», ТЗ п.9): профіль
   отримав nr-ГІЛКУ — другий прохід оцінки епізодів по тих самих
   сегментах, але БЕЗ ratio-гейта (лише ≥$100k; глибина не потрібна,
   nodepth не блокує). PROFILE_ALGO_V 7→8 (кеш перерахується). f7_ok =
   nr.ok + ті самі гейти цілісності/віку, що f4_ok. Вхід лише на
   першому пострілі; вихід 2×nr.avg_gap_s (кламп 60..300с); prof_*
   колонки рядка F7 несуть nr-числа. /strat2 profiles.ok_nr; UI
   «Профілів F4·F7: ok·ok_nr/total», S_FIRST={F5,F6,F7} — крива
   когорти перших пострілів на всіх трьох.
5. Фікси аудиту покриття: (а) update_watchlist отримав гард
   len(depth_snap)<10 (як у check_position_changes) — порожня глибина
   більше не може стерти весь watchlist; (б) sweep ПІДХОПЛЮЄ нові
   монети відомого кита з тієї самої відповіді clearinghouseState
   (ratio≥2 → у watchlist одразу, курсор філів на знімок; раніше нова
   монета чекала повного скану до 30+ хв при НУЛІ додаткових запитів);
   (в) /positions і /wallets серіалізують JSON і пишуть у сокет ПОЗА
   cache_lock (під локом лише знімок посилань; cache.data/depth/wallets
   замінюються цілком, ніколи не мутуються на місці) — одна відкрита
   вкладка на повільному каналі більше не гальмує обробник трейдів;
   (г) VIP-скан: топ-VIP_TOP_N=2000 лідерборду за accountValue
   сканується КОЖЕН скан без smart-skip (кит із $50M між позиціями —
   не мертвий акаунт; ціна ~+6.5% циклу, лог «VIP повернуто зі скіпу»).
6. Версії: DATA_ALGO_V "2.8"→"2.9" (нова популяція входів), PROFILE 8.
СВІДОМО НЕ ЗРОБЛЕНО: рознесення скана лідерборда і sweep у часі
(джерело 429 у пікові хвилини) — користувач сказав «окремо подивитись
пізніше»; правильний фікс — спільний бюджет ваги основної IP (як
PROFILE_W_PER_MIN), окремим патчем.

## v2.10 (04.09): дев'ятий зовнішній аудит — 8 підтверджених + 5 важливих

Найсильніший із зовнішніх аудитів (усе звірено з кодом, більшість
відтворювані). Регресія scratchpad/test_v210.py (9 блоків); вся батарея
14 сюїт зелена. DATA_ALGO_V "2.9"→"2.10", PROFILE_ALGO_V 8→9.
1. Гейт MIN_POS_USD при ПОВНОМУ закритті — по позиції ЕПІЗОДУ
   (ep["val"]), не по залишку перед фінальним батчем: $5k-хвіст
   $200k-зливу більше не глушить R1-R7/_OUTCOME. Часткові сигнали —
   по old["val"], як раніше.
2. R7 «одним пострілом» повністю переведений на ЕПІЗОД: fc_episodes
   тепер несуть start_size (токени на старті серії) і найбільшу tx
   усього епізоду (max_sz/max_usd/max_liq); частка рахується в
   ТОКЕНАХ (max_sz ≥ 95% start_size) — рух ціни під час зливу не дає
   ні хибних спрацювань, ні пропусків; велика tx у попередньому
   батчі більше не губиться. $100k — окремий поріг по нотіоналу tx.
   Фолбек без епізоду (рестарт) — стара батч-перевірка.
3. Ratio-гейт (≥2) доданий у rev_on_close і follow_on_txs — те саме
   правило, що v1.3 тримає для алертів: carry-пара з ratio<2 більше
   не відкриває paper-позицій і не пише outcome/сигнальні рядки.
   Мітка пари (перший постріл) оновлюється незалежно.
4. Часткові закриття оновлюють ratio за СВІЖОЮ глибиною
   (_fresh_ratio, усі 3 місця size/val) — «ratio на момент алерту»
   тепер чесний і в realtime-шляху, не лише в скан-діфі.
5. Системні філи (ліквідації/ADL) з нульовим hash 0x000…0 отримують
   стабільний sys-id (гаманець+монета+oid/tid): різні ліквідації
   більше не склеюються у фальшивий «великий шматок», а глобальний
   дедуп alerted_txs не гасить чужі ліквідації годину; ADL
   («Auto-Delever…») класифікується як закриття і в live (раніше —
   лише у профілі).
6. «Перший постріл» — властивість ТРАНЗАКЦІЇ: старша tx у тому
   самому батчі (навіть дрібна) з'їдає перший постріл до великої.
7. База порогів «≥5%» і tx_pct — startPosition САМОЇ транзакції
   (число біржі: позиція перед нею), з фолбеком на стару базу:
   4 + 4.9 токена від 100 — друга чесно 4.9/96=5.1%. Узгоджено з
   v2.6 №2: sp — СУЧАСНА транзакції база, це не «перекваліфікація
   старої tx меншим залишком».
8. Профіль: «Short > Long» закриває ШОРТ (глибина з ask) — стара
   гілка хапала слово «Long». PROFILE_ALGO_V 9 — кеш перерахується.
Додатково: fc_episodes персистяться у state.json (рестарт посеред
5-хв зливу більше не губить старт епізоду; seen set↔list), знімок
fc_* — під fc_lock (гонка «dict changed size» валила весь снапшот);
_strat_csv_append відмовляє рядку чужої ширини (голосно, без запису);
load_leaderboard переживає list/порожню відповідь (APIError замість
креша, watchlist живе через carry).
ВІДХИЛЕНО з аудиту (свідомо): env-фолбек для токена/проксі — легальний
(повторно, як у попередніх аудитах); FC-трекер і далі відкидає
однотранзакційні закриття (dur=0) — легасі-порівнянність із 40
історичними епізодами, одношотові події тепер повністю покриває R7;
pair_gap лишається за часом ПІДТВЕРДЖЕННЯ (узгоджено з таймерами
тиші виходу — зміна на біржовий час розсинхронізувала б їх);
тести в репо/zip не постачаються (живуть у scratchpad сесії — межа
задокументована тут).

## v2.11 (08.09): 7 пунктів ТЗ — грейс ratio, версії статистики, швидкість, F8/F9, TWAP-реверс, мобільна версія, проксі скану

Регресія scratchpad/test_v211.py (8 блоків: 7 пунктів ТЗ + блок 8 на
кожну знахідку рев'ю, включно з парсером на 40 живих постах каналів);
батарея 16 сюїт зелена. DATA_ALGO_V "2.10"→"2.11".
1. ГРЕЙС-ВІКНО ratio (RATIO_GRACE_S=1800): пара, що мала ratio ≥2,
   ще пів години після падіння нижче 2 гейтиться як велика — усі
   закриття ≥5% алертяться і пишуться у rev/follow (друга половина
   зливу «2.5 → закрив половину → 1.25» більше не губиться). Не встиг
   закрити за 30 хв — знову «мала» до повернення ratio ≥2. Механіка:
   _mark_ratio() — єдина точка запису ratio (ставить ratio_hi_ts при
   ≥2; усі 4 місця вставки пари і 4 місця оновлення); _ratio_ok() —
   єдиний гейт в обох алерт-шляхах, rev_on_close, follow_on_txs;
   скан-діф успадковує мітку з попереднього знімка (пара з ratio<2
   поза грейсом у базу діфу не потрапляє — інакше 10k позицій). Алерт-
   шлях при full_close гейтить розмір по позиції ЕПІЗОДУ (_ev_val
   читається ДО fc_on_full_close), як rev у v2.10.
2. СТАТИСТИКА ПЕРЕЖИВАЄ АПДЕЙТИ: версія порівнюється як кортеж
   (_vt: 2.9 < 2.10); у кожної стратегії — своя since-версія
   (STRAT_SINCE: остання версія, від якої її логіка і спільний
   сигнальний шар не мінялись; нові — версія появи), спільні стрічки —
   TAPE_SINCE. API читає поточний CSV + УСІ *.legacy-<ts>.csv (кеш по
   mtime/size), фільтрує per-strategy; legacy_rows = відкинуті версією.
   ПРАВИЛО СУПРОВОДУ: змінив логіку стратегії або спільний шар, що
   міняє її популяцію входів — підніми її since; додав — since =
   поточна. Зараз усі R1–R7/F1–F7 since=2.10 (грейс п.1 лише ДОДАЄ
   раніше загублені події тієї ж семантики — свідомо не бамп; рядки
   несуть ratio, когорту можна відрізати post-hoc), F8/F9/T1/T2 = 2.11.
3. ШВИДКІСТЬ: per_day = входів / дні від ПЕРШОГО рядка стратегії
   (мін. 1 день), days — на картці «швидкість: X угод/день · за N д ·
   логіка з vSINCE».
4. F8_ratio35 («перший постріл · ratio ≥3.5») = F5 + ratio пари на
   момент входу ≥3.5. F9_без_ратіо_90 = F7 (nr-гілка) з ≥90% швидких
   замість 70% — рахується з nr.n_fast/fast_pct на льоту, профіль без
   бампа (PROFILE_ALGO_V лишається 9).
5. TWAP-РЕВЕРС (T1_твап_відкриття / T2_твап_скорочення). Джерело —
   веб-прев'ю t.me/s/HL_TWAP і t.me/s/TWAPx (stdlib urllib + regex, без
   бота-адміна/MTProto; після 3 збоїв читання — через REST-проксі),
   опитування кожні TWAP_POLL_S=30с, last post id у state.json. Парсер
   twap_parse(): обидва формати старту, cancel (HL «TWAP is cancelled
   [x/y] + тіло», HL «$X TWAP with COIN closed by user», TWAPx «TWAP
   отменён + Субъект/Размер»), HL «successful completed»; стійкий до
   «сплющених» постів (t.me прибирає переноси у частині постів); час —
   HL Period у UTC+N (місяці словником, не %b), TWAPx «Создан в:
   HH:MM:SS (UTC)» + дата поста. У розмітці t.me пост-відповідь несе
   ДВА div-и tgme_widget_message_text: спершу цитата (js-message_reply_
   text), потім текст (js-message_text) — _tme_parse бере саме
   js-message_text і повертає цитату 4-м полем: HL «closed by user» без
   адреси в тексті бере бік/адресу з цитати стартового поста (адреса там
   може бути обрізана — _twap_find звіряє за префіксом і найближчою
   сумою). HL НЕ постить окремого «completed»/«cancelled» — він РЕДАГУЄ
   стартовий пост, тому вотчер перечитує вже бачені pid лише як
   cancel/done (ідемпотентно). Назва монети — як у Hyperliquid
   (_coin_canon по поллеру цін: kPEPE, не KPEPE). Реєстр twap_reg
   (дедуп по гаманець+монета+бік+старт ±2 хв ЛИШЕ між каналами: новий
   пост того ж каналу = новий твап — кит скасував і перестворив).
   Беремо ЛИШЕ ≤TWAP_MAX_DUR_S=15 хв з адресою. kind: clearinghouseState
   через prio-канал — позиції немає або того ж боку → open (T1),
   протилежного → reduce (T2); стійко до запізнення поста. Стан-машина
   (_twap_tick): за TWAP_ENTRY_LEAD=60с до кінця — p0 = ціна за
   хвилину ДО старту (px_min: похвилинна історія 4 год; фолбек — ціна
   з поста, p0_src), p1 = зараз (передостання хвилина), рух у бік
   твапу ≥TWAP_MIN_MOVE=1% → вхід ПРОТИ (buy→SHORT, sell→LONG),
   трекер у rev_open з track_min=120 (run_strat2_loop горизонт тепер
   per-tracker), headline m60 = «вихід через годину», крива 120 хв;
   рух < 1% → move_small; скасування до входу → dropped, після —
   cancel_after_entry=1 у рядку; спізнення >90с → late. CSV:
   twap_signals.csv (КОЖЕН побачений твап: eligible/kind/p0/p1/move/
   result) і twap_trades.csv (24 поля + m1..m120 + eol). API: T1/T2
   kind=twap, cohorts {1.0,1.5,2.0} — ті самі входи по руху (кнопки
   «рух ≥X%» у деталях, окремих трекерів немає), drops по причинах,
   twap.{watching,recent,channels}; /status twap_*. Реєстр і last_ids
   у state.json; записи старші 6 год після кінця — прюнінг.
   Свідомі межі: перший запуск бере лише пости молодші за годину;
   HL-репост без Period (сплющений) як старт ігнорується (є повний
   пост); p0 з поста — не «хвилина до старту», позначено p0_src.
6. МОБІЛЬНА ВЕРСІЯ (≤900px, ≤480px): одна колонка, монети —
   горизонтальна стрічка чипів, таблиці скроляться в контейнері,
   watch/події під ними; картки стратегій у стовпчик; крива ловить
   тач (curveTouch) і МАСШТАБУЄТЬСЯ (svg viewBox — без нього на 390px
   вона обрізалась, знайдено скріншотом); кнопки ≥36px; switchView
   ставить display='' (вирішує CSS). Перевірено Playwright 390×844:
   без горизонтального скролу сторінки, без JS-помилок.
7. ПРОКСІ СКАНУ (scan_proxy.txt, формат host:port:user:pass; .gitignore,
   у zip): fetch_one іде через hl_post_scan з ВЛАСНИМ бюджетом ваги
   SCAN_W_PER_MIN=1000/хв (clearinghouseState=2 → стеля ~8 гам/с; ліміт
   біржі 1200/хв на IP, тому «15–20 гам/с» фізично недосяжні на одній
   проксі) і БЕЗ поступання fast-перевіркам (інша IP); 429 → штраф у
   бюджет; 3 мережеві збої поспіль І понад 20с без жодної успішної
   відповіді (last_ok; паралельні воркери падають пачкою — один чих
   мережі не має ховати проксі) → проксі мертва SCAN_DEAD_S=30 хв, скан
   на основній IP зі старим тротлінгом, воркери «в польоті» після
   смерті streak не накручують; пейсер бюджету — у process() ДО знімка
   _t_fetch (інакше курсор філів випереджав запит на ~хвилину); проба на
   старті ([SCAN]); /status scan_proxy_*. Основна IP лишається
   обхіднику (~433/хв).
Не змінено свідомо: pair_gap за часом підтвердження; FC dur=0.

РЕВ'Ю v2.11 (Workflow: 5 лінз → 5 скептиків, усе підтверджене
виправлено, регресії — test_v211.py блок 8):
- TWAP критичне: регекс _tme_parse брав reply-цитату замість тексту —
  усі «closed by user»/«завершён» читались як старти (див. п.5);
  TWAPx «✅ TWAP завершён» не розпізнавався; дедуп у межах одного
  каналу зливав два твапи; .upper() ламав kPEPE; сторінка без постів
  рахувалась «тишею», а не збоєм; twap_active у /status без локу.
- Грейс: скан ставив ratio_hi_ts = старт скану (з'їдав до 20+ хв
  грейсу) — тепер час читання гаманця (fetch_times); пара у грейсі з
  ratio<2 у знімку випадала з watchlist — тепер лишається з мітками з
  живого watchlist; злиття бере свіжішу мітку з двох джерел;
  check_position_changes бере мітку і з живого watchlist.
- Версії: rev per_day ділив на дні від першого ВХОДУ (1 вхід через 9
  днів = «1/день») — тепер від першого сигналу; legacy читається ПЕРЕД
  поточним (dedup «останній виграє» → поточний файл перемагає);
  рядки нижче мінімальної since-версії не тримаються в пам'яті, лише
  рахуються; карантин/збої legacy — окремі лічильники
  legacy_quarantined/legacy_read_errors/legacy_files; кеш чистить
  видалені файли; текст UI без «перейменовано у *.legacy».
- Проксі скану: див. п.7 (last_ok, in-flight, пейсер до _t_fetch).
- Мобільна: main#termView 1fr → minmax(0,1fr) (сторінка скролилась
  вбік на ширину таблиці); ширина кривої від контейнера (680 у
  viewBox на 390px = нечитабельні підписи); підпис «120хв» зрізався
  (R=16 + text-anchor end); підказка у правій половині — ліворуч від
  пальця; touch-action:pan-y без preventDefault (скрол сторінки живе);
  ontouchcancel; стани loading/error у стрічці монет на всю ширину;
  .tabbtn/#sortBy ≥36px. Перевірено Playwright 390×844 з тач-подіями.

## v2.12 (08.09): рішення користувача після порівняння з CH-версією + перенесене з неї

Контекст: користувач приніс альтернативну реалізацію v2.11 (zip
«whale_terminal_2_11_CH», інша лінія, на базі v2.10, з власними модулями
twap_engine/patch_support/telegram_reader, 83 unit-тести). Порівняння
(з живими запитами до біржі й запуском їхніх тестів) — у сесії 08.09;
висновок: TWAP-верифікація, скан і модель версій там кращі, як продукт
для цього VPS — гірше (Telethon-логін, bind 127.0.0.1 у config, без
фолбеку проксі, crash на битому state). Користувач вирішив: F8 = копія
F6 + ratio≥3.5; грейс — від останнього великого закриття; версії —
наші (STRAT_SINCE); «візьми з тієї версії все, де він кращий». Регресія
scratchpad/test_v212.py (6 блоків) + переписаний TWAP-блок test_v211;
батарея 17 сюїт зелена; Playwright 390×844 з панелями. DATA_ALGO_V
"2.12"; STRAT_SINCE: F8 → 2.12 (інша популяція), T1/T2 → 2.12 (вид зі
startPosition, ціни зі свічок); заголовки twap_*.csv змінились (ротація).
1. ГРЕЙС ВІД ОСТАННЬОГО ВЕЛИКОГО ЗАКРИТТЯ: _mark_close(p, ts) зсуває
   ratio_hi_ts на час останньої великої tx батчу (≥5% і ≥$5k), якщо пара
   в цей момент проходила гейт — серія «злив 50% → ratio 1.3 → через
   25 хв ще 10%» живе від останнього шматка (мітка лише вперед; при
   повному закритті пара зникає). Сайти: check_wallet_worker (одразу
   після big_txs), check_position_changes (скан-діф, і в базу діфу, і в
   живий watchlist). ЯКІР big_val (з CH): _mark_ratio при ratio≥2 пише
   вартість позиції; _grace_val(p) = max(val, big_val) у грейсі —
   поріг $50k у трьох гейтах (алерт _ev_val, rev _event_val, follow)
   більше не ріже $30k-залишок після зливу $200k. big_val успадковують
   усі 6 місць вставки пари, update_watchlist (new_wl і злиття — max),
   check_position_changes (prev/живий watchlist). ПЕРЕВІДКРИТТЯ (той
   самий розмір, інший entry) скидає ratio_hi_ts/big_val: нова позиція
   не успадковує чужий грейс.
2. F8 = КОПІЯ F6 + ratio≥3.5: у follow_on_txs — у списку _timers
   (перший постріл, таймер F6_TIMER_S, профіль не потрібен, busy-скіп
   видимий як у F6); з _prof_strats прибрано. F9: поріг 90% по ТОЧНИХ
   n_fast/n_slow (100·n_fast ≥ 90·(n_fast+n_slow)), а не по округленому
   fast_pct (89.99 записано як 90.0).
3. СКАН (з CH): _discover_pairs(addr, positions, fetched_at) — нова
   велика пара у watchlist одразу після читання гаманця (ratio по
   поточній глибині, курсор філів на момент знімка, ratio_hi_ts/big_val
   = знімок, tombstone після знімка не воскрешаємо, глибина <10 монет —
   нічого); update_watchlist у кінці скану лишається (видалення/злиття).
   Каденс: _schedule_scan(max(5, REFRESH_S − тривалість)) — від СТАРТУ
   скану, а не від кінця (90-хв скан + 30 хв паузи = 2-годинний цикл
   раніше). should_skip: слот = sha256(addr) mod CHECK_EVERY — хронічно
   порожні перевіряються п'ятими частинами щоскану, а не всі 40k у
   кожному п'ятому. /status: scan_next_in_s, scan_last_duration_s,
   scan_new_pairs_live.
4. TWAP — ЗВІРКА З БІРЖЕЮ (з CH; формати перевірено живими запитами):
   _twap_verify(rec): twapHistory (запис монети/боку зі start ±10с →
   twapId, стан activated/finished/terminated/error, sz/executedSz) +
   userTwapSliceFills (перший слайс цього twapId → startPosition =
   позиція ДО твапу; слайс з'являється через ~1–2с після створення):
   0 → open (T1, нова позиція), протилежний бік і sz ≤ |позиції| →
   reduce (T2), той самий бік → increase, більше за позицію → flip —
   обидва dropped not_new_or_reduce. Стара евристика по поточній
   позиції прибрана (доливи йшли в T1 — обидва «5-хвилинні» твапи
   перевіреного гаманця були доливами). Вид — ЛИШЕ зі звірки
   (kind_src="slice"), інакше kind_unknown. Дедуп по twapId
   (_twap_dedup_by_id: старший запис лишається, пости/канали зливаються).
   Стан з біржі: terminated до входу → cancelled, після → cancel_after_
   entry; finished з executed≥sz → completed=1 (у запису і в трекері —
   колонка completed у twap_trades). Після кінця — легка звірка (без
   слайсів) раз на 30с до 5 хв. Ціни: P0 = закриття останньої повної
   1-хв свічки біржі перед стартом, P1 — перед останньою хвилиною
   (candleSnapshot через prio-канал, кеш 1 год); фолбек — поллер
   (p0_src/p1_src: candle/hist/msg/live). Ціна paper-входу — жива.
   TWAP <2 хв — ineligible dur<2m (немає передостанньої хвилини).
   Скасування: _tme_parse віддає 5-й елемент reply_pid (href у
   tgme_widget_message_reply) → _twap_by_post зіставляє точно; HL-edit
   старту (seen) — по власному pid; евристика по сумі/цитаті — фолбек.
   Нові колонки: twap_signals kind_src/exch_id/start_pos/p0_src/p1_src,
   twap_trades ті самі + completed. Бюджет: ~2–3 запити по 20+ ваги на
   твап (історія+слайси при реєстрації, свічки ×2, підтвердження).
5. СТАН: SIGTERM/SIGINT → save_state → вихід (systemctl restart не
   губить до 60с стану); atexit теж; startup у main() під
   `if __name__ == "__main__"` (модуль імпортується без запуску потоків).
   load_state при стані >1 год: ринковий знімок (watchlist, курсори,
   епізоди, sim) пропускається, але rev_open/follow_open/twap_reg/
   twap_last_ids відновлюються — цикл трекерів пише "" у пропущені
   хвилини, простій = чесні прогалини, а не втрачена угода.
6. МОБІЛЬНА (з CH): термінал — три панелі монети/позиції/watch+події з
   перемикачем (body[data-pane], mobilePane(), вибір монети → позиції),
   стрічка монет прибрана; кнопки ≥44px, липкий хедер, touch-action:
   manipulation (без 300мс), focus-visible, prefers-reduced-motion;
   «↑ До стратегій» у деталях і автоскрол до деталей після вибору картки.
СВІДОМО НЕ ВЗЯТО з CH: Telethon-читач (потрібен api_id/логін; наше
t.me/s/ працює одразу — див. v2.11 п.5); серверні кнопки-стратегії
≥1.5/≥2% (наші когорти — підмножина тих самих входів, дані не губляться
поки «вимкнено»); реєстр відбитків правил (користувач обрав STRAT_SINCE);
відсутність фолбеку скану на основну IP; crash на битому state.json;
SQLite-журнал повідомлень; ваговий бюджет prio-каналу (гальмує профілі).

## v2.13 (08.09): відповідь на зовнішню критику v2.12 (34 сценарії)

Користувач приніс критику v2.12 («корисні зміни зберегти, TWAP ще
потребує виправлення»). Усі 8 пунктів перевірені по коду й підтверджені;
виправлено все, крім свідомих рішень (нижче). Регресія
scratchpad/test_v213.py (8 блоків, ~35 сценаріїв з фейковою біржею у
реальному форматі відповідей), TWAP-блок test_v211 оновлено; батарея
18 сюїт зелена; Playwright 390×844. DATA_ALGO_V "2.13"; STRAT_SINCE:
T1/T2 і когорти → 2.13 (вихід при скасуванні, когорти-стратегії,
первинність слайсу); заголовки twap_trades змінились (ротація).
1. ВХІД У ВІДОМО СКАСОВАНИЙ ТВАП (критичне): _twap_apply_exchange
   тепер викликається на КОЖНОМУ тику для кожного запису, після
   звірки при читанні поста (_twap_resolve_kind) і ще раз
   безпосередньо перед входом (разом зі свіжою звіркою історії).
2. ІДЕНТИЧНІСТЬ: (a) прив'язка скасування по reply-посту/власному pid
   ОСТАТОЧНА — знайдений запис у стані dropped означає «уже оброблено»,
   а не «шукай евристикою іншого» (перечитуваний кожні 30с пост гасив
   другий запис); (b) без відомого twapId кандидат з twapHistory має
   збігатись і за тривалістю в хвилинах (±1), обирається найближчий за
   стартом, два різні twapId на відстані <3с — «ambiguous», не вгадуємо;
   (c) дедуп між каналами при реєстрації — лише за однакової тривалості
   (±60с) і суми (±15%), серед кількох — найближчий за стартом (раніше
   $100k/10хв і $1M/15хв за 60с зливались; 30-хв непридатний поглинав
   10-хв придатний).
3. МОНЕТА ВІЛЬНА ПІСЛЯ ГОДИННОГО ХОЛДУ: busy у _twap_enter — лише
   трекери тієї ж стратегії на монеті з now − entry_ts < 60 хв;
   120-хв спостереження монету не тримає. КОГОРТИ ≥1/≥1.5/≥2% — тепер
   ОКРЕМІ стратегії з власними трекерами (T1_твап_відкриття,
   _15, _20; так само T2): базова більше не забирає в когорти ≥2%
   пізніший сигнал. Кнопки в UI перемикають стратегії; в API —
   cohorts{thr} з власними n/per_day/open_now і cohort_names.
4. СКАСУВАННЯ ПІСЛЯ ВХОДУ = ВИХІД: exit_min = наступна хвилина після
   скасування (перша з ціною, ≤60), exit_reason=cancelled, net60 —
   на цій хвилині. Вибірки розділені: головна — усі фактичні paper-
   входи; окремо n_confirmed/median_confirmed/win_confirmed
   (біржа finished + виконано повністю + без скасування);
   n_cancelled, n_partial (finished, але executed<sz — таке буває:
   кінець часу ≠ повний розмір), n_unconfirmed (без відповіді біржі за
   5 хв), n_candle (обидві ціни зі свічок). Колонки exch_status/
   exec_pct/exit_min/exit_reason у twap_trades. UI: окремий блок у
   деталях T1/T2, статус у таблиці угод.
5. ЧАС І ЦІНА ВХОДУ: TWAP_LATE_S 90→15 (вхід лише в останню хвилину);
   перед входом час читається заново (now2) і late перевіряється ще
   раз — блокуючі запити не дають входу заднім числом; ціна входу —
   ЛИШЕ жива (_px_now), без неї — no_price (свічка P1 — для руху);
   P0 більше не береться з ціни поста (інша методика) — свічка або
   поллер; порожня відповідь candleSnapshot НЕ кешується.
6. СТАН: битий state.json → перейменовується у .corrupt-<ts>, стан
   відновлюється з state.json.bak (копія раз на 10 хв при збереженні);
   заморожений final_row TWAP старої схеми перебудовується з тих самих
   семплів (_twap_row) замість 201 відмови й дропу.
7. RATIO ПОВНОГО ЗАКРИТТЯ — на СТАРТІ епізоду (fc_episodes.ratio) в
   алерті («Ratio на старті епізоду: 2.5x (зараз 1.25x)») і в рядку
   rev_on_close; часткові — поточний. ШВИДКІСТЬ: n = рядки + відкриті
   зараз трекери (open_now в API); горизонт — від min(активація since-
   версії стратегії, перший рядок): strat_activated у state.json,
   _init_strat_activation() при старті (нова since-версія = нова
   активація), мінімум 1 день.
8. ЗВІРКА: перший слайс — лише якщо не пізніше TWAP_FIRST_SLICE_S=35с
   після старту (кап 2000 виконань) і з startPosition; інакше
   kind_src="unproven" → drop kind_unproven (не «0 = нова позиція»).
   Кількість запитів: звірка доки вид невідомий (кеш 10с), одна перед
   входом, після кінця раз на 60с до 5 хв — ≈≤8 історій за твап
   (було ~29). Скан: повторна спроба clearinghouseState теж резервує
   вагу 2. Рух рівно 1% (0.07→0.0707 у float) проходить (епсилон).
РЕВ'Ю ДИФУ v2.13 (адверсарійне, по коду + живій сторінці t.me/s/HL_TWAP;
4 знахідки, всі виправлено, test_v213 блок 9):
- пост скасування БЕЗ прив'язки (старт відредаговано у «cancelled» ще до
  першого читання, старт старший за годинний зріз, запис уже вичищений)
  при перечитуванні кожні 30с зіставлявся евристикою з НОВИМ твапом
  того ж кита (живі дані: PONS 1702-1705 — чотири $4.0m за 5-27с) →
  евристика лише при ПЕРШОМУ читанні; знайдений нею запис дістає цей
  пост у posts, далі — точна прив'язка;
- хвилина виходу при скасуванні після входу — з часу ревізії біржі
  (twapHistory.time, у СЕКУНДАХ; state.timestamp — у мс), коли він
  пізніший за вхід: після рестарту «хвилина, коли дізнались» була
  пізніша за фактичне скасування; біржа може лише ЗМЕНШИТИ exit_min;
- порожня candleSnapshot: після TWAP_CANDLE_TRIES=3 порожніх відповідей
  за бар — більше не запитується (монета без свічок тягнула вагу 20
  щотику все життя твапу);
- підпис «Ratio на старті епізоду» в алерті — лише коли ratio справді
  епізодний (ratio_src="episode"); скан-діф і фолбек показують «Ratio».
СВІДОМО ЛИШЕНО (обговорено з користувачем/критиком): грейс ковзний від
останнього великого закриття (рішення 08.09); фолбек скану на основну IP
при смерті проксі (видимий у /status scan_proxy_alive, зі старим
тротлінгом); входи з цінами не зі свічок лишаються у головній вибірці,
але їх кількість видно (n_candle) і джерела в кожному рядку.
Первинна суперечність «увійти в останню хвилину і знати, що твап
завершиться» вирішена як у критиці: попередній вхід + підтвердження
біржею після кінця, окрема підтверджена вибірка.

## v2.14 (08.09): відповідь на зовнішній аудит v2.13 (38 сценаріїв)

Аудитор підтвердив виправлення v2.13 і знайшов 8 груп дефектів, що
міняють ЗАПИСАНИЙ результат або ховають угоди. Усі звірені з кодом —
точні; виправлено все. Регресія scratchpad/test_v214.py (9 блоків,
~40 сценаріїв: фейкова біржа, керований годинник, реальні CSV/state у
tempdir); батарея 19 сюїт зелена. DATA_ALGO_V "2.14"; STRAT_SINCE
T1/T2 і когорти → 2.14 (нижче); R/F не змінювались (рядки сумісні).
1. НЕЗМІННІСТЬ ЗАВЕРШЕНИХ УГОД (критичне, стосувалось і 2.12): аварія
   між записом рядка у CSV і save_state → рестарт відновлював трекер зі
   state і закривав угоду ВДРУГЕ за поточною ціною; дедуп «останній
   виграє» підміняв результат (−1.15% → −20.15%). Тепер: (а) load_state
   звіряє відновлювані трекери rev/follow/twap/outcome з ключами вже
   записаних рядків усіх стрічок (_csv_written_keys: поточні файли +
   .legacy; огризки без вартового — не доказ) і НЕ відновлює записані
   (лог «уже записані у CSV»); (б) дедуп у strat2_api — ПЕРШИЙ валідний
   рядок виграє (огризки відкидає _read_file, тож «останній» більше не
   потрібен); дублі рахуються (dup_rows в API); (в) save_state після
   запису рядків — синхронно в циклі трекерів, не у фоновому потоці.
2. ІДЕНТИЧНІСТЬ TWAP: (а) фінальні пости несуть дані біржі — TWAPx:
   TwapId, Статус, Исполнено/Размер; HL_TWAP: [exec/total] у
   відредагованому старті, «Filled: a/b» і «Time: … GMT» у «closed by
   user». Парсер їх читає (twap_id, status, exec_sz, target_sz, fin_ts).
   Cancel/done зіставляється: reply/власний пост → twap_id з поста →
   (перше читання) кандидати гаманця/монети без id ЗВІРЯЮТЬСЯ з біржею
   і збігаються ПО ID; без id (HL «closed by user») — лише за ЄДИНОГО
   кандидата або єдиного збігу суми (<0.1%; сума в пості — 3 значущі
   цифри); два схожі активні твапи — «неоднозначно», нічого не гасимо
   (fin_ambiguous/fin_unmatched у twap_stats), біржа розсудить перед
   входом. «Новіша активна заявка» більше ніколи не вгадується.
   (б) дедуп між каналами при реєстрації — старт ±20с (обидва канали
   дають час біржі; було ±120с — перестворена за 30с заявка зливалась)
   і тривалість ±30с (15 і 16 хв — різні); у dropped (скасований)
   запис не зливаємо — нова заявка отримує власну звірку, а справжній
   дубль знімає _twap_dedup_by_id по twapId біржі (тепер і з dropped
   старшим записом; скасування не рахується двічі).
3. COMPLETED = ПОВНЕ ВИКОНАННЯ: «завершено» з каналу ставить completed
   лише коли пост показує exec≥total; інакше tg_done + tg_partial;
   біржа finished з executed<sz ЗНІМАЄ completed, поставлений каналом.
   Без відповіді біржі 5 хв при tg_done — статус tg_done (не
   unconfirmed і не confirmed). Час скасування з HL-поста (Time:) —
   хвилина виходу, коли біржа ще не відповіла.
4. СПИСКИ УГОД КОГОРТ: cohorts[thr].trades — власний для кожної
   когорти-стратегії (+n_pending); UI бере його замість фільтра базової
   по руху (це різні входи).
5. УГОДА ≠ СПОСТЕРЕЖЕННЯ: _twap_close_min/_twap_trade_closed — угода
   закрита на хвилині скасування (≤60) або m60; закрита угода не
   тримає монету (busy лише до закриття: скасування на 1-й хвилині
   звільняє одразу, не через годину), не рахується в open_now, а її
   результат з'являється одразу як pending-рядок (_twap_row із
   семплів трекера, pending=1) — крива до 120 хв дописується у CSV
   при завершенні трекера, дедуп по ключу не дає подвоїти.
6. ЦІНА НА МЕЖІ ХВИЛИНИ: цикл трекерів не закріплює пропуск «», доки
   триває 30-с допуск (наступний тик за 3с може принести ціну); m60
   без ціни у TWAP → paper-вихід першою наступною хвилиною з ціною,
   exit_reason=timer_late (явна позначка); rev: m30 порожній → net30 з
   першої з m31..m33, trade.late_exit, n_late у блоці стратегії.
7. ВХІД ЛИШЕ ЗІ СВІЖОЮ УСПІШНОЮ ЗВІРКОЮ: verified_ts — спроба,
   verify_ok_ts — успіх (реальний годинник); перед входом потрібна
   успішна звірка не старша за TWAP_VERIFY_MAX_AGE_S=90с, інакше
   наступний тик пробує ще, а коли він уже не встигає у вікно —
   пропуск no_verify (verify_fail у записі). Входу «за старим статусом»
   немає; режим без підтвердження свідомо не введено.
8. СТАН/ОБЛІК: state.json відсутній, а .bak є → відновлення з .bak;
   валідний JSON не dict ([]) → .corrupt-<ts> + .bak; міграція з папки
   коду в /home/hl_data переносить УСІ *.csv (з .legacy-ротаціями і
   twap_*), state.json(.bak/.corrupt-*), профілі, секрети — і логує
   кількість; open_now/швидкість рахують лише трекери з algo_v ≥ since
   стратегії і лише ті, чиїх ключів ще нема у CSV (унікальний облік).
ДРІБНЕ: рух за час твапу — неокруглений (1.49996% — не когорта ≥1.5%;
округлення лише в CSV); TWAP_FIRST_SLICE_S 35→25с (інтервал слайсів
30с — філ ≤25с не може бути другим) + якщо відповідь userTwapSliceFills
повна (2000) і не сягає старту твапу — unproven; вотчер t.me після
пропуску догортає старіші сторінки (?before=, ≤3) до останнього
баченого id або годинного зрізу; ваговий бюджет prio-каналу СПІЛЬНИЙ
(облік у hl_post_prio: 2 для легких типів, 20 (+1/20 елементів) для
решти; проксі 800/хв; TWAP чекає бюджет ≤5с, легкі запити ≤3с);
опис допуску: вхід в останню хвилину твапу, до 15 с після кінця
(TWAP_LATE_S — явний параметр, не «повернення часу»).
РЕВ'Ю ДИФУ v2.14 (адверсарійне; критичних не знайдено, 6 дрібних —
виправлено): no_verify у розкладі пропусків API/UI; статус із поста
йде в exch лише разом із цифрами виконання (інакше tg_status); допуск
збігу суми — піводиниці 3-ї значущої цифри поста ($1.00m: ±5k), не
0.1%; dropped-запис З twapId зливається з дублем поста другого каналу
(без id — ні); резерв бюджету для сторінки історії — 120 (w_next);
угода закрита за записаним семплом хвилини виходу або після 30-с
допуску (pending-рядок на межі 60-ї хвилини не порожній).
СВІДОМО ЛИШЕНО: ковзний 30-хв грейс, повернення скану на основну IP,
ручний STRAT_SINCE (як і раніше); повний ваговий планувальник між
каналами і SQLite-транзакції — після накопичення даних.

## v2.15 (09.09): відповідь на зовнішній аудит v2.14 (53 сценарії)

Аудитор підтвердив виправлення v2.14 і показав 8 груп дефектів обліку
TWAP: результат, показаний до запису, міг змінитись після аварії; вихід
заднім числом; хибний перший філ; ідентичність; неузгоджене закриття;
бюджет без резерву; загублений пропуск t.me; крайові стани відновлення.
Усі підтверджені по коду й виправлені (плюс дві зміни методики). Регресія
scratchpad/test_v215.py (8 блоків: цикл трекерів з аварією, фейкова біржа,
керований годинник, реальні CSV/state у tempdir); батарея 20 сюїт зелена;
Playwright 390×844. DATA_ALGO_V "2.15"; STRAT_SINCE T1/T2 і когорти →
2.15 (рядки T1/T2 з 2.14 не порівнянні: інша хвилина виходу при
скасуванні, інший запис); заголовки twap_trades змінились (ротація у
.legacy) — тепер 37 полів + m1..m60; НОВИЙ файл twap_curves.csv.
1. НЕЗМІННИЙ ЗАПИС УГОДИ (критичне): рядок twap_trades.csv пишеться у
   момент ВИРІШЕНОГО paper-виходу (цикл трекерів, tw_rows), трекер далі
   веде лише криву до 120 хв → twap_curves.csv (key twap_id|strategy)
   при завершенні; трекер видаляється лише після ОБОХ записів (угода
   перша). Аварія після показаного результату більше не перераховує
   його: load_state бачить ключ угоди у CSV → відновлює трекер з
   trade_written=1 (рядок не переписується), ключ кривої у CSV → не
   відновлює. pending-синтез в API прибрано (рядки приходять з CSV);
   крива в API — з twap_curves по ключу, поки її нема — перші 60 хв з
   рядка угоди. Restored v2.14-трекери: done без записаної угоди →
   спершу угода, потім крива.
2. ВИХІД НЕ РАНІШЕ ОТРИМАННЯ СИГНАЛУ: exit_min при скасуванні = хвилина,
   коли бот ДІЗНАВСЯ — max(наступна після поточного семплу, поточна
   хвилина за годинником; семпли можуть відставати у 30-с допуску); час
   події з біржі/поста — cancel_event_min (окрема колонка, дослідження);
   хвилина, коли дізнались, — cancel_seen_min (≠ exit_min при
   cancelled_late: тоді exit_min — перша наступна хвилина з ціною);
   закриту угоду (рядок записаний/заморожений) пізнє скасування не
   рухає. v2.14 виходила заднім числом за ціною, якої ще не могла знати.
3. ПЕРШИЙ ФІЛ — ЗА ЛАНЦЮГОМ ПОЗИЦІЙ (_twap_chain_head): end =
   startPosition ± sz за боком; голова — філ, чий startPosition не є end
   жодного іншого; рівно одна голова → вона, інакше unproven. tid —
   хеш, не порядковий номер: два філи одного слайсу з однаковим часом
   робили open → increase, reduce → flip.
4. ІДЕНТИЧНІСТЬ: у dropped-записи не зливаємо взагалі (відомий id старої
   заявки не доводить, що новий пост про неї); справжній дубль знімає
   _twap_dedup_by_id по id біржі БЕЗ другого сигналу (sig_written,
   starts/eligible не рахуються, dup_posts у twap_stats). Сума з поста
   — обмеження ЗАВЖДИ (±2%), і за повної адреси: «closed by user
   $2.84m» більше не гасить єдиний відомий $100k-твап гаманця.
5. ЄДИНИЙ ФАКТ ВИХОДУ (_twap_exit): ціна хвилини виходу → вона; нема —
   перша наступна в межах TWAP_EXIT_CAP_MIN=5 (timer_late /
   cancelled_late, і після 60-ї хв); нема й після капу — no_price
   (результат порожній). Він керує записом рядка, зайнятістю монети
   (нова угода не входить, доки вихід попередньої не відомий — без
   накладання), open_now і списком відкритих (out["open"]: закрита
   угода з кривою — не «у ринку»). Після заморозки рядка вирішений
   вихід зберігається й у трекері (tw["exit_final"]=[min, reason]) —
   крива бере його звідти, не з позиції колонки.
6. БЮДЖЕТ PRIO-КАНАЛУ: резерв ваги АТОМАРНО під локом у момент
   рішення, на кожну спробу (429 теж рахується); після max_wait —
   RateLimited («budget exhausted»), запиту немає; фактична вага понад
   резерв дописується. Профіль резервує 120 за сторінку. prio-воркер
   ловить RateLimited окремо: «budget» у prio_fetch.csv, dropped+1,
   кулдаун адреси ~75 с (не «мертва проксі», не 10-хв глухота).
7. ДОГОРТАННЯ t.me: незавершені пропуски — ЧЕРГА st["gaps"] =
   [[lo,hi,created_ts],…] (id у (lo,hi) не читані; новіший першим:
   його пости ще можуть бути живими заявками), догортається наступними
   циклами (≤3 сторінок/цикл) до старого last або годинного зрізу;
   новий пропуск під час догортання старого — окремий запис, не
   втрата; пропуск старший за годину знімається як історія; пости з
   пропуску — нові (seen=False), а не «бачені». Черга переживає рестарт
   (state.json: twap_gaps); відповідь (cancel/done) на пост усередині ще
   не догорнутого пропуску відкладається до його закриття (st["deferred"]);
   три збої поспіль на тому самому before= знімають пропуск. /strat2:
   twap.channels[ch].gaps.
8. СТАН/МІГРАЦІЯ/ЖУРНАЛ: _read перевіряє типи полів state (saved_at —
   число; rev_open/follow_open/twap_reg/… — dict; списки — list) →
   інакше .corrupt + .bak; міграція з папки коду при однойменному CSV у
   DATA_DIR: той самий заголовок → дописати рядки, інший →
   <fn>.legacy-migrated-<ts>.csv (читач legacy підхопить), оригінал →
   .migrated-<ts>; відмова запису сигнального рядка TWAP → outbox
   _sig_retry_q (ретрай у циклі), а не втрата. Лічильники twap_stats у
   /status: dup_posts, fin_ambiguous, fin_unmatched (id нікому не
   належить), fin_nomatch (без id і без кандидата).
РЕВ'Ю v2.15 (6 агентів по вимірах дифу, кожен із пробами на коді):
1 high — дубль СКАСОВАНОГО твапу з другого каналу зливався у ПЕРЕСТВОРЕНУ
живу заявку кита (start ±20с), і reply-скасування дубля гасило її:
тепер _twap_register бере найближчого кандидата ВКЛЮЧНО з dropped і, якщо
найближчий dropped, — новий запис (дубль зніме id); пост з id біржі, чий
reply-запис має інший id, не гасить його (post_id_mismatch). 2 medium —
_twap_dedup_by_id був недосяжний через реальний ingest (звірка з посту
одразу давала slice) → тепер у _twap_resolve_kind до apply_exchange і на
тику для всіх watch з id; справжній 429 біржі на prio-каналі не давав
бек-офу → штраф півстелі вікна, як у скану, і «429» окремо від «budget» у
prio_fetch.csv; єдиний кандидат без id — теж лише в межах одиниці 3-ї
значущої цифри суми. low — заморожений trade_row старої ширини
перебудовується (не вічна відмова), twfail > WFAIL_CAP → дроп; exit_ts
відновленого done-трекера = хвилина виходу, не рестарт; крива без
exit_final бере _twap_exit; twap_gaps у state; n_candle лише з net;
лічильники twap_stats з нуля; direct-сторінка профілю — одна спроба на
резерв.
МЕТОДИКА: пізній вихід R (m31..m33 при порожньому m30) — лише для
рядків algo_v ≥ 2.14 (старі R-рядки інтерпретуються як раніше);
P1 з live-ціни при відсутній свічці — лишено свідомо (видно у
p1_src/n_candle).
СВІДОМО ЛИШЕНО: ковзний 30-хв грейс, повернення скану на основну IP,
ручний STRAT_SINCE; SQLite-транзакції — після накопичення даних.

## v2.16 (11.09): чесна ціна paper-угод, settlement по стрічці Binance, R8, епізодне правило реверсу, грейс/періоди/когорти, 11 знахідок рев'ю

ТЗ користувача 11.09 (8 пунктів) після аудиту реалізму цін: paper-вхід
«за китом» брався з кешу мідів (allMids раз на 5 с), який міг ПЕРЕДУВАТИ
філу кита — follow-SHORT входив вище за ринок (оптимістично), реверс-LONG
— вище (песимістично), TWAP — майже без ефекту. Регресія
scratchpad/test_v216.py (8 блоків); батарея 22 сюїт зелена (старі сюїти
адаптовані через v28_shim: стаб _paper_px = чистий мід, гейти віку філа
вимкнені, _rev_ref_px відтворює старий 3-хв рух); Playwright 1440/390.
DATA_ALGO_V "2.16"; STRAT_SINCE R1–R7 → 2.16 і R8 = 2.16 (нове правило
кваліфікації + чесна ціна — старі R-рядки не порівнянні; див. п.2 як вони
повертаються через settlement); F* і T* без бампа (рядки сумісні: ті самі
події, лише чесніша ціна — видно у entry_src/lag_s). Заголовки ВСІХ CSV
змінились (ротація у .legacy): rev_signals +6, rev_trades +20 (після
algo_v, перед m1), follow_trades +12, follow_outcomes +5, rev_outcomes +8,
twap_trades +6 (43 поля + m1..m60). НОВИЙ модуль settle.py і файл
settlements.csv у DATA_DIR.
1. ЧЕСНИЙ PAPER-ВХІД/ВИХІД (п.1): у момент рішення — свіжий стакан HL
   (l2Book через prio-канал, вага 2, до 3 с очікування бюджету):
   hl_book_exec = виконувана VWAP ринкового ордера на $1000 з НАШОГО
   боку (BUY по асках, SELL по бідах, partial якщо стакан тонший);
   _paper_px → src 'book'; без стакану — ГІРША з кеш-міда (≤20 с) і ціни
   філа кита ('mid+whale' / 'mid'); нема й міда → входу немає
   (rev_no_px/follow_no_px). Виходи теж зі стакану: follow (тиша /
   full_close), R m30 (перша хвилина з ціною у 30..33, далі no_price),
   R8 TP (лімітка: не краще за TP і не краще за стакан), R2 armed→open
   (гірша з стакану і тригера), TWAP (m60/скасування). Рішення — під
   strat2_lock, HTTP-запит ціни — ПОЗА локом (exit_req/arm_hit/
   exit_pending → друга фаза під локом; застарілий запит після аварії
   переставляється). У КОЖНОМУ рядку: entry_ts_ms, fill_ts_ms (час філа
   кита з біржі), lag_s (рішення − філ), detect_src (ws/sweep),
   entry_src, entry_px_mid (мід на момент рішення), px_age_ms, whale_px,
   exit_src/exit_px/exit_ts_ms (R: exit_min/exit_reason). Гейти віку
   філа: follow >FOLLOW_MAX_FILL_AGE_S=20 с → paper-вхід пропущено
   (follow_stale_skips), rev >REV_MAX_FILL_AGE_S=60 с → лише outcome
   (rev_stale_skips) — «вхід» через хвилину після філа не є входом.
2. SETTLEMENT (п.2–3, settle.py + run_settle_worker кожні 120 с, ≤40 угод
   за цикл, старт через 90 с): кожна ЗАКРИТА paper-угода (follow / rev /
   twap, поточні файли + .legacy) детермінованo переоцінюється по стрічці
   aggTrades Binance USDⓈ-M — денні zip з data.binance.vision (кеш на
   диску, LRU 24 символо-дні в RAM) + REST /fapi/v1/aggTrades за останні
   48 год (10-хв відра, зберігаються лише повні): ціна = ГІРША для нас у
   вікні [t, t+3 с] → [t, t+10 с] → останній трейд ≤60 с (stale) → none;
   вхід від entry_ts_ms (старі рядки — від локального date), вихід follow
   від close_ts_ms, R — рівно +30 хв (R2 — реплей тригера +0.3% у 10 хв,
   no_trigger якщо стрічка його не бачила; R8 — TP 0.8×дамп або таймер),
   TWAP — entry + exit_min; net_tape = gross − ті самі costs_pct рядка.
   Філи кита — HL userFillsByTime через prio-канал (кеш по годинах):
   lag_s, епізод закриття (паузи ≤120 с) → dump_first/last_ts, dump_dur,
   dump_bucket, dump_move_pct (референс і кінець — зі стрічки Binance
   перед першим філом / на останньому, прапорець dump_tape; лише філи
   — dump_fills). settlements.csv: ключ = ключ рядка (trade_id;
   sig_id|strategy; twap_id|strategy), settle_v, entry/exit live і tape,
   flags (no_tape/partial/no_trigger/no_dump/no_fills/no_whale_fill).
   ОФІЦІЙНИЙ РЕЗУЛЬТАТ (рахує strat2_api): net_official = min(net_live,
   net_tape); без стрічки — live з прапорцем. Кожна угода в API несе
   net30 (=офіційний), net_live, net_tape, settled, flags; блок стратегії
   — n_settled/n_unsettled, live/tape-зрізи тієї ж вибірки; хедер вкладки
   «Розрахунок · стрічка». Не втрачаємо 7 днів R-даних: старі R-рядки
   (2.10–2.15) ДОПУСКАЮТЬСЯ у статистику стратегії, якщо settlement
   відновив епізод і він проходить нове правило (bucket 1..5 і рух ≥
   поріг стратегії; n_adm_legacy, у таблиці adm=1) — популяція стає
   такою ж, як у нових рядків. CLI: python3 settle.py --data-dir
   /home/hl_data [--since] [--limit] [--force]. У пісочниці fapi
   відповідає 451 — REST працює лише з VPS; zip-и доступні звідусіль.
3. ГРЕЙС (п.4): колонка grace у рядках R/F (1 = пара пройшла ratio-гейт
   лише завдяки 30-хв грейсу, ratio<2 на вході; для старих рядків API
   виводить із ratio<2); n_grace у блоці; UI-кнопки «грейс / без грейсу»
   (як «волти»), зріз по показаних.
4. ПЕРІОДИ (п.5): periods.today (від локальної півночі) / d7 / d30 / all
   по кожній стратегії (n, медіана, середнє, win, PnL) на офіційному net
   без пізніх виходів; таблиця у деталях.
5. R8_тп80 «Реверс · TP 80% відкату» (п.6): вхід як R1, tp_px =
   entry×(1 ± 0.8×дамп) — вихід, коли мід перетинає TP (ціна зі стакану,
   не краще за TP), інакше таймер m30; крива до m60 ведеться далі.
6. НОВЕ ПРАВИЛО РЕВЕРСУ (п.7, усі R): кваліфікація за ЕПІЗОДОМ закриття
   кита, а не 3-хв вікном кешу: референс = мід ДО першого close-філа
   (_rev_ref_px: 5-с семпл не пізніший за філ, ≤90 с; нема — ціна
   першого філа), кінець = ціна останнього філа; падіння ≥1% у бік
   закриття (R4 ≥2, R5 ≥3) і тривалість <REV_EP_MAX_S=300 с; когорта
   dump_bucket = ceil(dur/60) 1..5 (0 = довше). move_3m_pct лишається
   довідковою колонкою. Сигнальний рядок і outcome несуть dump_dur_s/
   dump_move_pct/dump_bucket. UI: кнопки «дамп: усі / 0–1 … 4–5 хв» на
   R-картках — серверний блок buckets[N] (офіційний net, уся вибірка) +
   фільтр таблиці. Зайнятість монети (busy) — лише поки заголовкова
   30-хв угода відкрита (_rev_trade_closed: exit_reason, або ≥30
   семплів, або 30 хв+35 с) — 60-хв крива монету не тримає
   (rev_busy_skips у /status).
7. 11 ЗНАХІДОК РЕВ'Ю (п.8): вік філа (п.1); busy 30 хв (п.6); вікно
   руху від філів, не від рішення (п.6); поллер allMids — одна спроба
   (4 ретраї з бекофом давали 16–44 с сліпоти), px_last_ok/px_fail/
   px_age_s у /status, watchdog: семпл цін старший за 60 с дві перевірки
   поспіль → TG-алерт; follow silence_late і НОВИЙ full_close_late
   (повне закриття, про яке дізнались >60 с після філа) — поза
   заголовковою вибіркою, n_late; підпис кривої follow чесний («один
   трек на активну пару»); швидкий шлях: філів ще нема → повторна
   ШВИДКА перевірка через 1.5 с (кап 3), а не sweep через 20–30 с;
   sweep чанками по 20 адрес зі зливом швидких подій між чанками;
   вихід full_close зі стакану в момент рішення; витрати по ОБОХ боках
   стакану (_sim_costs: вхід по своєму боку, вихід по протилежному,
   зберігаються у трекері); KLUNC→1000LUNCUSDT, KDOGS; лише символи
   status=TRADING; DEPTH_LIMIT 500→1000 (500 рівнів обрізали 1% для
   HYPE/ZEC — ratio роздутий), пейсер глибини 1.5 с; делістнуті монети
   HL не опитуються; штамп семпла = час запиту; стрибок міда >25% чекає
   підтвердження наступним семплом; _px_at лише семпли ДО точки (без
   підглядання для P0 TWAP); «показано X з N (усього M)»; R без ціни
   виходу — «без ціни».
РЕВ'Ю v2.16 (5 агентів по лінзах: хуки/ціни, цикл трекерів, API+settlement,
інфраструктура, UI; кожна знахідка відтворена пробою) — виправлено:
- HIGH: fast-retry вимикався назавжди після 3 повторів на пару (лічильник
  не скидався, коли філи знайдено) → скидання при підтверджених філах,
  прюнінг мертвих пар; TWAP-вихід після рестарту переоцінювався стаканом
  «зараз» під міткою m60 → якщо хвилина виходу давно минула — рядок із
  семплів, як у v2.15; черга settlement голодувала (рядки, що дають None,
  займали limit) → limit лише на спроби, новіші угоди першими, рядки <2.10
  не беруться; settlement-запити HL (вага 20) з'їдали prio-бюджет
  paper-входів і звірки TWAP → кап 12/цикл, пауза 2 с, кеш поточної
  години в пам'яті циклу.
- MEDIUM: px_min зберігав підлогу хвилини з ціною ПЕРШОГО семпла хвилини
  (міг бути після філа → референс дампу «з майбутнього», одним пострілом
  = 0%) → справжні мітки часу; один запит стакану на (монета, бік) за
  прохід + кап 6 запитів/12 с (шість R-трекерів одного сигналу виходили на
  m30 одним тиком — тик тривав хвилину, інші трекери губили семпли);
  невдала ціна → повтор через 15 с, не щотику; _twap_enter після
  блокуючого запиту стакану перевіряє вікно входу ще раз; вихід R
  зберігається у state одразу (аварія до періодичного save_state
  переоцінювала його); тик, що проґавив 30..33 (рестарт), бере ціну із
  семпла m30 (exit_src=sample), не no_price; трекери v2.15 після деплою
  лишаються на старій семантиці m30; закрита R-угода, що веде криву, —
  не «у ринку»; 429/418 Binance → пауза за Retry-After, решта циклу
  глибини пропускається (негайний ретрай вів до бану IP); REST-стрічка
  settlement 1 запит/2 с (спільна вага IP з глибиною ≤1600/хв);
  транзиторний збій джерела (мережа/429/бюджет) — fetcher кидає
  settle.Transient, рядок відкладається (раніше фіксувався no_tape/
  no_fills назавжди); 404 zip кешується годину; монета, що вибула з
  Binance-всесвіту, видаляється з кешу глибини; допуск старих R-рядків —
  і за віком філа (lag_s ≤60 с, як live); офіційний net: R2 no_trigger і
  рядок без live-результату → без результату (консервативно);
  settlements.csv net_live для рядків v2.16 — від ціни виходу зі стакану,
  як в API; sim_all_mids лишає ретраї для легасі SIM/FC (одна спроба —
  лише у поллері); стакан: обов'язковий лише наш бік, partial → гірший
  пройдений рівень (src book_partial); LRU стрічки 24→6 символо-днів,
  zip-и старші за 14 днів видаляються.
- LOW/UI: зріз «по показаних» без пізніх виходів (як заголовок); «>5 хв»
  сортується як найдовший і має кнопку; клітинка «Дамп» = діапазон, як
  кнопки; колір live/стрічка; знак від'ємного PnL; дата без переносу;
  пороги R з епсилоном; entry_ts реверсу — час, коли ціна відома;
  /status settle_pending — бек-лог; викид міда: монотонне продовження
  руху приймається другим семплом.
АУДИТ-2 v2.16 (11.09, зовнішній, 16 знахідок по zip 2_16, sha ff4ba356…)
— усе звірено з кодом і виправлено (регресія test_v216 блок 9; settle:
test_settle):
1. Запасний шлях входу (мід / ціна філа кита) ПРИБРАНО для paper-угод:
   вхід — лише за повним свіжим стаканом (strict): без стакану follow не
   входить (follow_no_book), реверс пише лише тіньову outcome-стрічку
   (rev_no_book, opened=""), TWAP — drop no_book; partial-стакан (<$1000
   на 20 рівнях) = нема стакану; біржовий час котирування l2Book старший
   за BOOK_MAX_AGE_S=5 с = не «зараз» (book_stale); вік міда міряється
   ПІСЛЯ запиту; quote_ts_ms — у журналі подій.
2. Свіжість follow — за ТРИГЕРНОЮ транзакцією (найбільшою придатною), не
   за найновішим дрібним філом батча.
3. Криві «хвилина виходу» — зі СТРІЧКИ (settlement curve_tape: ті самі
   ціна/витрати, що офіційний net) коли ≥3 угод, інакше live-семпли з
   явним підписом; F без стрічки — тіньова стрічка, як раніше.
4. Фільтри (грейс/волти/штанга/когорта дампу) — на СЕРВЕРІ по всій
   вибірці: /strat2_slice?st&grace&vault&sht&bucket → той самий агрегатор
   (_agg_block), що й картка: n/медіана/…/періоди/крива; UI при фільтрах
   показує зріз «усі угоди за фільтрами». Grace старих рядків (без
   колонки) = НЕВІДОМО (не виводиться з ratio: старий ratio — старт
   епізоду), у «без грейсу» не входить.
5. Єдине правило епізоду live/replay: сусідні транзакції з розривом
   ≤300 с І позиція між ними не зростала (startPosition нової ≈ залишок
   після попередньої, ±1%); долив/перевідкриття/фліп/тихе закриття
   скидають fc_episodes (раніше — лише close_episodes) і base R7 (№7).
6. Кінець зливу — СИРА ціна останнього філа ордера (px_last; VWAP
   ордера, що пройшов рівні, ховав кінець), початок — сира перша
   (px_first); референс «до закриття» — семпл не старший за
   REV_REF_TOL_S=12 с, інакше сира ціна першого філа (консервативно,
   без «дампу з минулого» від 80-секундного міда).
8. Повний replay старої історії неможливий без первинного журналу —
   відтепер він пишеться: events-YYYYMMDD.jsonl у DATA_DIR (close_batch
   із сирими цінами/розмірами/startPosition кожної транзакції, rev_decision
   з референсом/стаканом/причинами, follow_entry/exit, rev_exit,
   rev_arm_entry). Старі F-рядки без гейта свіжості: за settlement-лагом
   >20 с — поза заголовком (n_stale). Охоплення старої історії показане
   явно (n_adm_legacy, n_stale); сигнали, яких старий детектор не бачив,
   не відновлюються — це зафіксовано як межа.
9–11. Settlement — АУДИТ ЗАПИСАНОГО виконання, не переухвалення: entry/
   exit у записані entry_ts_ms/exit_ts_ms (v2.16 рядки), R2-тригер і
   R8-TP на стрічці — лише прапорці (no_trigger, tp_tape_hit/miss);
   трейд ДО моменту входу не є доказом (stale прибрано: нема трейдів у
   [t, t+10 с] = no_tape); статуси verified / live_only / tape_only /
   none / pending; офіційний = гірший з ДОСТУПНИХ оцінок (записаний
   збиток R2 без тригера на стрічці не зникає: live_only + розбіжність);
   no_tape для угод молодших за 72 год не фіксується (zip ще не
   викладений) — відкладається. UI показує окремо «верифіковано (обидві
   оцінки)» і лічильники live/стрічка/розбіжність. Стрічка Binance ≠
   стакан HL — це явно інший майданчик: перевірка «чи існувала така ціна
   на ліквідному ринку», не відтворення нашого $1000-ордера.
12. Витрати за ногою (_leg_costs): нога зі стакану вже містить спред і
   вплив — модельний сліпаж додається лише до mid/семпл-ніг; колонка
   costs_pct лишається повною моделлю (її ж застосовує стрічка).
13. Follow-вихід (повне закриття / таймер) не залежить від поллера мідів
   — ціну дає стакан; без стакану і міда — повтор через 15 с.
14. R2: дедлайн перевіряється ЩЕ РАЗ при фіксації входу після запиту
   стакану; без стакану вхід не фіксується.
15. TWAP: пізні виходи (timer_late/cancelled_late) — поза заголовком,
   як R/F (n_late).
16. R6 (волт) — лише ПОВНЕ закриття, як усі реверси (шматок ≥5% без
   повного закриття — не сигнал); партіал ≥30% — тінь src=partial.
Дрібне: _tape_last_px строго ДО t; порядок трейдів в одній мс — за id
угоди, не за ціною; прапорець обрізання 1%-діапазону глибини (trunc) і
вік кешу глибини (>2 цикли = невідома); зріз «по показаних» замінено
серверним; статус у таблиці угод.
ВИЗНАНІ МЕЖІ: TP R8 перевіряється по міду з 5-с поллера (не по тику
стрічки) — settlement реплеїть TP по стрічці; при WS-бані (детекція
лише sweep) гейт 20 с відсікає більшість follow-входів — це і є суть
правила (вхід через 25 с після філа не є входом), лічильник
follow_stale_skips покаже масштаб; REST-стрічка для монет із десятками
тисяч трейдів на 10 хв (ZEC) повільна (цілі відра сторінками) — угоди
<48 год доїжджають, але не за хвилини; R m30 — вихід на тику з
семплом 30-ї хвилини (≤30 с після межі), стрічка рахує рівно +30 хв;
референс дампу live — 5-с семпл HL, settlement — трейд Binance (базис
між біржами до ~0.1–0.3% на тонких монетах); транзиторний збій мережі
при settlement пише no_tape (перерахунок --force); admitted старі R-рядки
мають m1..m60 від старого (за мідом) входу — офіційний net їх ріже через
min(live, tape).

## Наступні кроки (узгоджені, чекають "роби")

1. tx1-варіант "TP1%/таймаут 5хв" — ЧАСТКОВО ПОГЛИНУТО v2.x: F1-F3 і
   follow_outcomes.csv вже збирають вхід на одиночній транзакції з
   ознаками; лишилась тільки мітка "чи переросло у повний злив" +
   TP-вихід, якщо перший тиждень покаже життя у F-стратегіях.
2. FC-тюнінг (60 хв, поріг 0.7, usd_s+штанга) — ПОГЛИНУТО v2.x
   повністю (REV-стрічка робить це краще); старий FC лишається як є
   для порівнянності з історичними 40 епізодами.
3. Тиждень живої звірки алертів проти фактичних закриттів.
4. Через тиждень: аналіз prio_fetch.csv (скільки китів зловили) і
   follow_outcomes.csv (крива хвилин для F) — рішення про проксі-пул
   для скану і TP-виходи на даних, не на здогадах.

## Робочі файли сесії (scratchpad, НЕ в репо)

Дані аналізу: hl_terminal/hl_terminal/{sim_trades.csv,fc_trades.csv,
terminal.log(22MB),state.json} (перша версія), upload4/ (свіжий знімок).
Актуальний деплой-zip: whale_terminal_2_16.zip (код + settle.py + tg_token +
tg_chat + rest_proxy + scan_proxy з аплоаду/повідомлення користувача; збирає
scratchpad/build_zip216.sh). Деплой — як завжди: unzip -o + restart +
/status (нові поля px_age_s, book_ok/book_fail, settle_*); через ~2 хв
після старту у /home/hl_data з'являться settlements.csv і кеш стрічки
(settle.py пише лише у DATA_DIR); старі rev/follow/twap CSV ротуються у
.legacy (заголовки змінились) — API читає їх далі.
